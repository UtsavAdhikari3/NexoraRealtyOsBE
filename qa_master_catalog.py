"""Single source of truth for Nexora RealtyOS master QA artifacts.

The catalog deliberately records current implementation behavior. Recommended
behavior is labeled in notes rather than being substituted for observable code.
"""

from __future__ import annotations

from collections import Counter
from datetime import date


TODAY = date(2026, 8, 9).isoformat()
ENVIRONMENT = (
    "Docker Compose: PostgreSQL 16, Django/Gunicorn API on :8000, scheduler loop, "
    "React/Vite agency UI, and Next.js storefront on :3000. Use browser DevTools, "
    "an API client, application logs, and read-only PostgreSQL access."
)


def spec(
    key, sheet, name, prefix, purpose, roles, ui, endpoints, models, prerequisites,
    data, dependencies, primary_action, primary_result, validation, permission_rule,
    filter_rule, delete_rule, duplicate_rule, db_impact, side_effects, specials,
):
    return {
        "key": key, "sheet": sheet, "name": name, "prefix": prefix,
        "purpose": purpose, "roles": roles, "ui": ui, "endpoints": endpoints,
        "models": models, "prerequisites": prerequisites, "data": data,
        "dependencies": dependencies, "primary_action": primary_action,
        "primary_result": primary_result, "validation": validation,
        "permission_rule": permission_rule, "filter_rule": filter_rule,
        "delete_rule": delete_rule, "duplicate_rule": duplicate_rule,
        "db_impact": db_impact, "side_effects": side_effects,
        "specials": specials,
    }


FEATURES = [
    spec(
        "auth", "01_Auth_Access", "Authentication and Access", "AUTH",
        "Register an agency owner, authenticate through password and optional login OTP, refresh JWTs, reset passwords, and reject access when a user or agency is no longer entitled.",
        "Public registrant; Agency Owner; Agency Manager; Agent; Super Admin",
        "/register, /login, /verify-login-otp, /forgot-password, /reset-password, /payment-required",
        "POST /api/auth/register/; POST /api/auth/login/; POST /api/auth/verify-login-otp/; POST /api/auth/resend-login-otp/; POST /api/auth/token/refresh/; POST /api/auth/password-reset/; POST /api/auth/password-reset/confirm/",
        "AgencyUser, Agency, LoginOTP, PasswordResetToken",
        "Email service configured for OTP/reset verification when exercised; two active paid agencies; one inactive user; one expired-subscription agency.",
        "Unique owner email; strong password; valid/invalid OTPs; expired and single-use reset tokens; valid/expired access and refresh JWTs.",
        "Agency subscription gate, email delivery, JWT authentication, frontend Zustand token persistence and Axios refresh interceptor",
        "Register a new agency owner, complete login/OTP, and open /dashboard.",
        "The owner and agency are created atomically, valid credentials produce the implemented login/OTP response, and authenticated API access succeeds only while the user and subscription remain active.",
        "Reject missing email/password, invalid credentials, weak passwords, malformed tokens, wrong/expired OTPs, and reused reset tokens without leaking account existence.",
        "Public endpoints are anonymous; application endpoints require JWT; super admin bypasses agency subscription; ordinary users must belong to an agency with an active subscription.",
        "N/A for authentication; verify throttling counters and consistent error envelopes.",
        "Logout is client-side token clearing. User soft deletion/deactivation must make subsequent authenticated requests fail; no hard-delete endpoint is exposed here.",
        "Repeated login/reset requests are throttled or return the same non-enumerating response; a reset token is single use.",
        "Registration adds Agency and AgencyUser records; OTP/reset operations add/update their token records; successful reset changes only the password hash; failed authentication must not change business data.",
        "Email may be sent; frontend stores/clears tokens; subscription/payment-required routing may change.",
        [
            ("Login throttling after repeated failures", "Security", "P0", "Agency user", "Send repeated incorrect passwords from the same throttle scope.", "Requests exceeding the configured authentication throttle are rejected; no token is issued.", "No user or agency rows change; throttle cache increments.", "POST /api/auth/login/ returns the configured throttle response (normally 429) after the limit.", "Login shows a safe retry message and remains signed out.", "Authentication telemetry only."),
            ("OTP resend invalidates or supersedes the previous challenge", "State Transition", "P1", "Agency user awaiting OTP", "Request a resend, then try the old and new OTP values.", "Only the currently valid OTP challenge can complete login according to LoginOTP state.", "LoginOTP attempt/expiry state updates; no duplicate user is created.", "Old OTP is rejected; valid current OTP returns tokens.", "OTP page preserves the email context and reports errors without exposing codes.", "OTP email is sent once per accepted resend."),
            ("Refresh token single-flight behavior in the UI", "Concurrency", "P0", "Authenticated agency user", "Expire the access token and trigger several API calls simultaneously.", "Axios creates one refresh request, retries queued calls with the new access token, and avoids a refresh storm.", "No business rows change.", "One POST /api/auth/token/refresh/; retried calls use the returned Bearer token.", "The user remains on the current page without duplicate error toasts.", "Auth store tokens rotate once."),
            ("Password reset account enumeration resistance", "Security", "P0", "Anonymous visitor", "Submit one registered and one unknown email address.", "Responses are indistinguishable and do not reveal whether either account exists.", "A reset token is created only for the registered account.", "POST /api/auth/password-reset/ returns the same safe response for both inputs.", "Identical confirmation is shown.", "Mail is attempted only for the known account; mail failure is not disclosed."),
        ],
    ),
    spec(
        "agency", "02_Agency_Settings", "Agency Profile, Website and Settings", "AGY",
        "Maintain the tenant profile, public website identity, custom domain, contact channels, publishing state, and agency-level configuration.",
        "Agency Owner; Agency Manager for permitted reads/updates; Super Admin; public visitor for published data",
        "/settings, /website-content, public /agency/:slug",
        "GET/PATCH /api/agencies/me/; GET/PATCH /api/agencies/localization/; GET /api/public/agencies/by-slug/{slug}/; GET /api/public/agencies/by-domain/; GET /api/public/agencies/{license_number}/; POST /api/public/agencies/{license_number}/contact/",
        "Agency, AgencyLocalizationSettings, PublicSubmission/Lead from contact capture",
        "Authenticated paid agency; second agency for tenant tests; DNS/domain test data; published and unpublished website fixtures.",
        "Agency name/license/slug, Nepal address, phones, branding assets, custom domain, public website flag, contact message.",
        "Authentication, public agency resolver, storefront, localization, leads/public submissions",
        "Update the agency profile and publish its public website.",
        "Editable profile fields persist for the current tenant and published public endpoints resolve only eligible agency data.",
        "Reject attempts to change protected payment fields, invalid timezone/domain/address values, malformed URLs, and required-field omissions.",
        "Owners can update profile data; agents must not change agency-wide settings; public endpoints expose only public fields for active/published agencies.",
        "Resolve by slug, license number, and Host/domain query without crossing tenants.",
        "No public hard-delete endpoint exists. Platform suspension is handled by Super Admin and must make the agency disappear publicly.",
        "Unique slug/license/domain constraints prevent conflicting agency identities; repeated contact submission behavior follows public lead deduplication.",
        "Agency row and one-to-one localization settings update; public contact may create PublicSubmission/Lead/Interaction records.",
        "Storefront metadata/content changes; public visibility can change; audit logging is expected for operations mutations where implemented.",
        [
            ("Protected payment fields cannot be patched by an owner", "Permission", "P0", "Agency Owner", "PATCH /api/agencies/me/ with payment_status or paid_at.", "The protected fields remain unchanged even if other allowed profile fields update.", "Agency payment_status and paid_at are unchanged.", "Response omits or ignores/rejects protected updates according to serializer behavior.", "Settings never presents payment status as an editable profile field.", "Subscription entitlement remains unchanged."),
            ("Unpublished website is unavailable publicly", "Security", "P0", "Public visitor", "Turn off website publication, then resolve the agency by slug and domain.", "Public resolution and customer registration are rejected/not found for the unpublished site.", "No public/customer rows are created.", "Public resolver returns the implementation's not-found response; protected customer registration is rejected.", "Storefront shows not found/unavailable rather than stale tenant content.", "Search engine metadata must not expose unpublished content."),
            ("Custom domain resolves the correct agency", "Integration", "P1", "Public visitor", "Call by-domain resolution with Agency A and Agency B Host values.", "Each domain returns only its configured active, published agency.", "No rows change.", "GET /api/public/agencies/by-domain/ returns 200 for the matching domain and 404 for unknown/unpublished domains.", "Tenant branding changes with the resolved agency.", "Subsequent property and agent calls use that agency identity."),
        ],
    ),
    spec(
        "team", "03_Team_Agents", "Team, Invitations and Agent Profiles", "TEAM",
        "Invite managers/agents, accept invitations, manage team access, and maintain public agent profiles without losing tenant or role controls.",
        "Agency Owner; Agency Manager; Agent (own profile); Super Admin; Public visitor",
        "/team, /agents, /my-profile, /accept-invitation, public agent pages",
        "GET/POST /api/agents/; GET/PATCH/DELETE /api/agents/{id}/; GET/PATCH /api/agents/me/profile/; /api/operations/invitations/; POST /api/operations/invitation/accept/; /api/operations/team-members/; GET /api/public/agencies/{license}/agents/",
        "AgencyUser, AgentProfile, Invitation, Notification, AuditLog",
        "Paid Agency A and B; owner, manager, two agents; SMTP capture; active, expired, and accepted invitation tokens.",
        "Unique invite email, roles agency_manager/agent, profile biography/license/languages, active/inactive states.",
        "Authentication, email, public storefront, property assignment, leads, audit log",
        "Invite an agent, accept the invitation, and complete the agent's profile.",
        "One agency-scoped user is created with the invited role, invitation becomes accepted, and the active agent can update only their own profile.",
        "Reject expired/reused tokens, duplicate email conflicts, invalid profile values, role injection, and cross-agency assignments.",
        "Only owner/manager manages invitations/team access; agent self-profile endpoint is agent-only; owner cannot change another user's role through the legacy agent PATCH; public hides inactive agents.",
        "Team and public agent lists are tenant-scoped; agents see/update only their own profile.",
        "Agent delete is soft deactivation in the agent endpoint; team rules preserve at least one active owner and prevent self-deactivation/role change.",
        "Double acceptance must not create two users; invitation email failure records delivery_error without losing the invitation.",
        "Invitation created then accepted_at set; AgencyUser/AgentProfile created; access changes update AgencyUser and AuditLog.",
        "Invite email; public agent visibility; assigned records remain linked but inactive agent access ends.",
        [
            ("Agency must retain one active owner", "Validation", "P0", "Agency Owner", "Attempt to deactivate or demote the only active owner through team-members.", "Request is rejected with the explicit retention rule.", "Owner role/is_active is unchanged; audit log is not created for a failed update.", "PATCH /api/operations/team-members/{id}/ returns 400.", "Team page displays the server validation message.", "Agency login continuity is preserved."),
            ("Agent cannot manage invitations", "Permission", "P0", "Agent", "List, create, update, or delete invitations directly through the API.", "Every action is forbidden.", "No invitation or audit rows change.", "Operations invitation endpoints return 403.", "Navigation/action controls are hidden or disabled; direct URL still fails.", "No email is sent."),
            ("Inactive agent disappears from public listing", "Integration", "P1", "Agency Owner and Public visitor", "Soft-delete/deactivate an agent who has a public profile, then refresh public agents and detail.", "Internal historical links remain; public list/detail no longer exposes the inactive agent.", "AgencyUser is_active becomes false; assigned business rows are not deleted.", "Authenticated delete succeeds; public detail returns 404 and list excludes the agent.", "Public team cards disappear without breaking property history.", "Authentication for old tokens is rejected."),
        ],
    ),
    spec(
        "property", "04_Property_Core", "Property Inventory and Lifecycle", "PROP",
        "Create agency-owned property inventory, assign listing agents, filter/search it, control publication, and preserve lifecycle/history rules.",
        "Agency Owner; Agency Manager; Agent with restricted mutation rights; Public visitor for eligible listings",
        "/properties, /properties/add, /properties/:id, /properties/:id/edit",
        "GET/POST /api/properties/; GET/PUT/PATCH/DELETE /api/properties/{id}/; GET /api/properties/filter-options/; public property list/detail endpoints",
        "Property, PropertyHistory, PropertyDuplicateFlag, PropertyMedia, AgencyUser",
        "Paid Agency A and B; owner/manager/agents; draft/available/reserved/negotiation/sold/rented/withdrawn properties.",
        "Valid property payload, assigned agent, publication flag, status, address and price; cross-tenant IDs.",
        "Agency, agents, Nepal data, media, freshness, verification, distribution, public storefront, leads",
        "Create a complete property as an owner and make it available/published.",
        "Property is tenant-scoped, receives derived identifiers/area data, records history/duplicate evaluation, and is public only when every eligibility condition passes.",
        "Reject missing title/type/purpose/address/price as defined by serializer, cross-agency agent IDs, invalid choices, and publish attempts from draft or by an agent.",
        "Owner/manager can update any tenant property; agent-created property is forced to draft and self-assigned; agent edits only assigned property and allowed fields; agent cannot publish, reassign, or delete.",
        "Filters cover type, status, city, neighbourhood, assigned/unassigned agent, search by title/location/ID, and canonical area ranges.",
        "Owner/manager delete is hard delete subject to relational on_delete rules; agent delete is forbidden; dependent protected records may block deletion.",
        "Duplicate detection flags likely matches rather than silently merging; repeated create without client idempotency can create another property and must be observed.",
        "Property +1; PropertyHistory and possible PropertyDuplicateFlag rows; failed transaction creates none; update changes only targeted property.",
        "Public visibility, dashboard counts, filter options, duplicate queue, distribution eligibility, and agent inventory can change.",
        [
            ("Agent-created property is forced to draft and self-assigned", "Permission", "P0", "Agent", "POST a property with status=available, is_published=true, and another assigned_agent.", "The created property is draft, unpublished, and assigned to the requesting agent.", "One Property plus history; no foreign agent assignment.", "POST /api/properties/ returns 201 with enforced fields.", "Detail page shows Draft and the current agent; publish controls are unavailable.", "Property is absent from public endpoints."),
            ("Draft cannot be directly published", "State Transition", "P0", "Agency Owner", "Create a draft, then PATCH only is_published=true without moving to an allowed availability state.", "Publication is rejected or remains false according to serializer validation.", "No public-eligibility timestamps are initialized; history reflects only accepted mutations.", "PATCH returns 400 for invalid publication state.", "Form presents the validation error and preserves unsaved inputs.", "Public list remains unchanged."),
            ("Cross-agency assigned agent ID is rejected", "Security", "P0", "Agency Owner A", "Create/update Property A using Agent B's ID.", "Assignment fails and no tenant relationship is formed.", "Property assigned_agent remains null/unchanged.", "POST/PATCH returns 400 or scoped not-found as implemented.", "Agent selector never offers Agent B; API tampering is still rejected.", "No notification/history for the rejected assignment."),
            ("Search and combined filters return deterministic tenant results", "API", "P1", "Agency Manager", "Combine property type, status, city, neighbourhood, assigned agent, and search filters.", "Only Agency A properties matching every supplied condition are returned in stable pagination.", "No rows change.", "GET /api/properties/ returns 200; invalid assigned_agent produces the implemented empty list.", "Filters remain reflected in the UI and result count.", "Filter-options endpoint remains consistent with available tenant data."),
        ],
    ),
    spec(
        "nepal_property", "05_Nepal_Property", "Nepal Property Data and Area Conversion", "NPD",
        "Represent Nepali land units, local addresses, road/access details, plot dimensions, utilities, classifications, price rates, and automatic unit conversion.",
        "Agency Owner; Agency Manager; assigned Agent; Public visitor (read-only fields)",
        "/properties/add steps, property details/edit, public property detail/filtering",
        "POST/PATCH /api/properties/; GET /api/properties/filter-options/; GET public property list/detail/filter-options",
        "Property canonical_area_sqft and structured Nepal fields",
        "Property form fixture for hill units, Terai units, square feet and square metres; known conversion values and NPR prices.",
        "Ropani/Aana/Paisa/Daam; Bigha/Kattha/Dhur; sqft/sqm; province/district/municipality/ward/tole; road, facing, mohada/pichhad, utilities.",
        "Property serializer/model conversion helpers, filtering, public payload, localization/price formatting",
        "Create equivalent plots using each supported unit family and compare canonical area and unit prices.",
        "Canonical square-foot area is mathematically consistent, conversion payloads and price-per-unit values agree, and structured Nepal fields round-trip to authenticated/public views.",
        "Reject negative area/road/distance values, unsupported units/choices, nonsensical ward/address combinations where validation exists, and zero-area price-rate division.",
        "Same property mutation rules as core inventory; public reads only eligible property data.",
        "Area range filtering uses canonical_area_sqft so equivalent units match consistently.",
        "Deleting the property removes the structured fields with the Property row; no separate geography catalog currently exists.",
        "Unit conversion is deterministic; repeated serialization must not drift or mutate stored input values.",
        "Property fields and canonical_area_sqft update; no extra conversion table rows are created.",
        "Price display, filter matching, brochures/exports, matching score, and public detail change.",
        [
            ("Hill unit conversion exactness", "Boundary", "P0", "Agency Manager", "Enter 1 ropani, 16 aana, 64 paisa, and 256 daam in separate otherwise-identical properties.", "Each representation produces the same canonical square-foot area within Decimal precision.", "Each Property stores its selected area input and equivalent canonical_area_sqft.", "Create responses expose consistent area_conversions.", "Details show equivalent values without binary floating-point artifacts.", "Price per aana/sqft values reconcile."),
            ("Terai unit conversion exactness", "Boundary", "P0", "Agency Manager", "Compare 1 bigha, 20 kattha, and 400 dhur.", "All canonical areas are equivalent according to the implementation conversion constants.", "No conversion drift on save/reload.", "API responses expose matching conversions.", "Unit selector and summary show correct Terai units.", "Portal CSV and brochures use the same values."),
            ("Square-metre and square-foot area filter equivalence", "Integration", "P1", "Agency user/Public visitor", "Create equal sqft and sqm properties, then filter around the canonical boundary.", "Both appear or disappear together at equivalent min/max bounds.", "No rows change during filtering.", "Authenticated/public list endpoints return consistent matches.", "Result count and displayed units remain correct.", "Matching and reports use the same canonical area."),
            ("Zero and negative measurements fail safely", "Validation", "P0", "Agency user", "Submit zero/negative area, road width, mohada, pichhad, or major-road distance values.", "Invalid values are rejected where validators apply; zero area never produces divide-by-zero output.", "No invalid property is committed; failed update leaves the old values.", "API returns 400 with field-level errors rather than 500.", "Form highlights the affected fields.", "No history, duplicate, or public changes from rejected data."),
        ],
    ),
    spec(
        "media", "06_Property_Media", "Property Media", "MEDIA",
        "Upload, list, order, mark primary, and delete tenant-scoped property images/media used by internal views, public listings, social publishing, and distribution assets.",
        "Agency Owner; Agency Manager; assigned Agent; unauthenticated user forbidden",
        "Property add/edit media step and property details gallery",
        "GET/POST /api/properties/{property_id}/media/; GET/PATCH/DELETE /api/properties/media/{id}/",
        "PropertyMedia, Property",
        "Agency A/B properties; assigned/unassigned agents; valid image files and invalid/oversized/unsafe fixtures.",
        "JPEG/PNG/WebP as accepted; invalid MIME/extension; primary flag; sort order; cross-tenant property/media IDs.",
        "File storage, property permissions, public gallery, distribution packages, Meta Instagram publishing",
        "Upload several images, set one primary, reorder, and remove a non-primary item.",
        "Media is attached only to the target tenant property; setting a primary image clears any previous primary flag; gallery order is stable.",
        "Reject unauthenticated, cross-tenant, unassigned-agent, malformed, unsupported, or size-violating uploads according to configured validators/storage.",
        "Authenticated tenant users can list; mutation follows property assignment rules; another agency cannot view or mutate media.",
        "List media by property in stored ordering; public serializers expose only media for publicly eligible properties.",
        "Delete removes the PropertyMedia row and storage object according to configured FileField storage; deleting primary may leave no primary unless replacement logic exists.",
        "Repeated upload creates separate media because no idempotency key/hash deduplication is evident; document this current behavior.",
        "PropertyMedia rows add/update/delete; other Property rows remain unchanged except primary normalization across sibling media.",
        "Public gallery, social creative, media package, brochure/QR assets and property cards can change.",
        [
            ("Only one primary media item remains", "Database", "P0", "Agency Manager", "Mark image A primary, then mark image B primary for the same property.", "B is primary and A is atomically unset; no second primary remains.", "Exactly one PropertyMedia row has is_primary=true for the property.", "PATCH media B returns 200 and a subsequent list reflects normalization.", "Gallery hero switches to B.", "Distribution toolkit chooses B for generated assets."),
            ("Cross-tenant media upload is blocked", "Security", "P0", "Agency user A", "POST a file to Agency B property_id and try GET/DELETE its media ID.", "All cross-tenant access fails without revealing B's media metadata.", "No B records/files change and no A record is created.", "Detail operations return scoped 404/permission denial; upload is rejected.", "UI cannot discover or display B's media.", "No generated asset cache should be affected."),
            ("Unsafe or unsupported upload fails safely", "Security", "P0", "Agency user", "Upload executable/polyglot/oversized content with misleading image extension.", "Application rejects invalid content where implemented and never renders it publicly; production virus scanning is not determined from current repo.", "No accepted PropertyMedia row for rejected file.", "API returns 400/415 rather than 500. Virus-scan behavior requires clarification.", "Safe validation message; no broken preview.", "No social/distribution processing occurs."),
        ],
    ),
    spec(
        "verification", "07_Verification", "Property Verification and Due Diligence", "VER",
        "Track a fixed Nepal document checklist and ordered verification milestones without exposing private documents publicly.",
        "Agency Owner; Agency Manager; assigned Agent for permitted document work; Public visitor sees summary only",
        "Property details verification panel",
        "GET/PATCH /api/properties/{id}/verification/; GET/PATCH/DELETE /api/properties/{id}/verification/documents/{document_type}/",
        "PropertyVerification, PropertyVerificationDocument, Property",
        "Tenant property; users in each role; files/URLs for all ten document types; second-tenant property.",
        "Lalpurja, owner citizenship/company registration, Napi Naksa, Char Killa, Malpot receipt, tax clearance, building approval, completion certificate, POA, marketing authorization.",
        "Property inventory, file storage, public property serializer, trust badge, audit/history",
        "Open verification, upload/check documents, progress sequential milestones, and mark fully verified only when prerequisites are satisfied.",
        "Exactly ten checklist rows initialize; document statuses and ordered milestones persist; fully_verified guard prevents skipping required prior milestones.",
        "Reject unknown document_type, invalid status, fully-verified shortcut, cross-tenant file, and received/approved claims that violate serializer requirements.",
        "All access is tenant scoped; public gets summary/level only and never file URLs, identity numbers, reviewer notes, or private checklist payload.",
        "N/A list is the fixed checklist returned by verification endpoint.",
        "Deleting/resetting a document returns it to missing/current endpoint behavior without deleting the verification root; property deletion cascades checklist records.",
        "Repeated GET is idempotent and must not create more than ten document rows; repeated upload updates the keyed document type.",
        "Verification root + fixed document rows initialize; document/milestone timestamps/reviewer fields update; no duplicate (verification, document_type).",
        "Public trust summary/badge, manager review queue, distribution copy, property history may change.",
        [
            ("Checklist initializes exactly once", "Idempotency", "P0", "Agency user", "Call GET verification repeatedly and concurrently for a new property.", "Every response contains the same ten document types and no duplicate rows are created.", "One PropertyVerification and ten unique PropertyVerificationDocument rows.", "GET returns 200 every time.", "Panel displays ten stable checklist items.", "No notification/history duplication."),
            ("Fully verified milestone cannot be skipped", "State Transition", "P0", "Agency Manager", "Attempt fully_verified before owner identity, ownership receipt, inspection, and document review milestones.", "Request is rejected with the sequential milestone rule.", "Verification flags/timestamps remain unchanged.", "PATCH verification returns 400.", "Milestone UI explains unmet prerequisites.", "Public verification level remains unchanged."),
            ("Public payload never exposes private document data", "Security", "P0", "Public visitor", "Fetch a verified property after uploading sensitive files/numbers and reviewer notes.", "Only safe verification summary/level is returned.", "No rows change.", "Public detail returns 200 without file paths, external URLs, document numbers or private notes.", "Trust badge may display; no download links are present.", "Browser/network cache contains no private fields."),
            ("Rejected document correction workflow", "Workflow", "P1", "Agency Manager", "Mark a received document rejected with notes, upload a correction, and move it through review/approved.", "Status progression and correction metadata are retained without changing unrelated checklist items.", "One keyed document row updates; verification root recalculates summary.", "PATCH calls return 200 for allowed statuses.", "Checklist clearly shows rejected then corrected state.", "Verification level updates only when milestone rules permit."),
        ],
    ),
    spec(
        "freshness", "08_Freshness_Trust", "Listing Freshness, Availability and Trust", "FRESH",
        "Keep listings recently confirmed, expire/hide stale stock, collect public reports, detect duplicates, record withdrawals/history, and require manager approval to republish.",
        "Agency Owner; Agency Manager; assigned Agent for confirmation/request; Public visitor for viewing/reporting",
        "Property details freshness/history/duplicate panels and public report action",
        "POST /api/properties/{id}/freshness/confirm/; POST /api/properties/{id}/republish/request/; POST /api/properties/{id}/republish/decision/; GET /api/properties/{id}/history/; GET /api/properties/{id}/duplicates/; PATCH /api/properties/duplicates/{id}/; POST public listing-report submission",
        "Property, PropertyHistory, PropertyDuplicateFlag, PublicSubmission, Notification",
        "Available/reserved/under-negotiation properties around expiry; owner/manager/agent; duplicate candidates; public visitor.",
        "Confirmation dates, expiry boundary, withdrawal reason, approval/rejection notes, report reason/contact.",
        "Scheduler process_listing_freshness, public visibility, leads, notifications, dashboard/distribution",
        "Publish an available listing, confirm it, let it expire, request republish, and have a manager decide.",
        "Freshness window initializes, reminders are deduplicated, expired listing becomes non-public, and only approved republish restores a fresh public listing.",
        "Reject withdrawal without reason, agent self-approval, invalid availability status, duplicate decision, cross-tenant report workflow access, and future/illogical date input.",
        "Agent may act only on assigned listing and request republish; owner/manager decides; public only reports and never sees private history/duplicates.",
        "History ordered by time; duplicate queues tenant scoped and filterable by status.",
        "Property delete cascades freshness history/flags; public reports remain according to their property on_delete behavior. Republish history is not deleted independently.",
        "Reminder and expiry jobs are idempotent per window; duplicate report/confirmation behavior must not multiply reminders/history unexpectedly.",
        "Property freshness dates/publication flags/status may update; history/notification/submission/duplicate rows add as appropriate.",
        "Public search visibility, trust promise, distribution links, active lead expectations, dashboard counts and notifications change.",
        [
            ("Expiry boundary hides listing automatically", "Boundary", "P0", "Scheduler/Public visitor", "Set expires_at just before, exactly at, and just after current Nepal-aware time; run freshness command and query public list.", "Expired/equal-boundary records are hidden according to queryset comparison; future record remains visible.", "Expired property publication/expiry state and history update once.", "Public list/detail exclude expired entries; scheduler exits successfully.", "Listing disappears on refresh and direct detail is unavailable.", "Reminder/manager alert is not duplicated."),
            ("Reserved and under-negotiation remain public availability states", "State Transition", "P1", "Agency Manager/Public visitor", "Move a fresh published property from available to reserved and under_negotiation.", "Both statuses remain visible publicly as implemented, with accurate availability label.", "Property status/history changes; freshness remains valid.", "PATCH succeeds; public list/detail return the record.", "Badges update without removing the listing.", "Inquiries remain attributable to the property."),
            ("Republish requires manager approval", "Permission", "P0", "Agent and Manager", "Agent requests republish, then tries decision endpoint; manager approves once.", "Agent decision is forbidden; manager approval resets eligible freshness/publication exactly once.", "Request/decision metadata and history update; one manager audit trail.", "Agent decision 403; manager decision 200.", "Agent sees pending state; manager sees approve/reject controls.", "Public listing returns only after approval."),
            ("Public listing report creates reviewable submission", "Integration", "P1", "Public visitor", "Report a currently public listing with a reason, then inspect agency submissions.", "A listing_report submission is created for the correct agency/property without exposing internal data.", "PublicSubmission +1; property itself is not automatically withdrawn.", "Public POST returns created response; authenticated submissions list includes it.", "Public user gets confirmation; manager sees new submission.", "Any linked lead/history behavior follows current implementation and must be recorded."),
        ],
    ),
    spec(
        "distribution", "09_Distribution", "Listing Distribution Toolkit", "DIST",
        "Generate bilingual copy, watermarked images, brochures, window cards, QR codes, short tracked links, portal CSV, social drafts, and downloadable media packages.",
        "Agency Owner; Agency Manager; assigned listing Agent",
        "Property details distribution panel and social media handoff",
        "GET /api/properties/{id}/distribution/; GET/POST /api/properties/{id}/distribution/links/; PATCH/DELETE /api/properties/distribution/links/{id}/; GET /api/properties/{id}/distribution/assets/{asset_type}/; GET /api/properties/distribution/portal-export/; POST /api/properties/{id}/distribution/social-draft/; GET /api/public/s/{code}/",
        "PropertyDistributionLink, PropertyEvent, SocialPost, PropertyMedia, Property",
        "Fresh published property with primary/media images; agency branding/localization; assigned and unassigned agents.",
        "Nepali/English captions; attribution source/medium/campaign; asset types image, story, brochure, window-card, QR, CSV, media packages.",
        "Property/public eligibility, media, localization, social posts, public inquiry/site-visit attribution",
        "Open the toolkit, generate/download each asset, create a short link, share it, and create a social draft.",
        "Assets are downloadable and tenant branded; short link resolves eligible property, increments click/event attribution, and social draft uses generated creative/caption.",
        "Reject unassigned-agent access, unknown asset type, invalid attribution values/URLs, non-public/ineligible distribution where guarded, and cross-tenant link IDs.",
        "Owners/managers can distribute tenant properties; agents only assigned properties; public can use an active short link without accessing management data.",
        "Portal export honors selected IDs and tenant/agent property scope.",
        "Deleting a management link disables its code; generated transient assets should not alter source media/property.",
        "One redirect increments click counters/events once per request; repeated asset generation must not duplicate properties/media. No download idempotency guarantee is required.",
        "DistributionLink rows add/update/delete; redirect increments click_count and PropertyEvent; social draft adds SocialPost; export/download mostly read-only.",
        "Lead and site-visit UTM/source attribution, analytics, public traffic and Meta publishing can change.",
        [
            ("Tracked short link preserves inquiry attribution", "End-to-End", "P0", "Public visitor", "Open a source-tagged short link, then submit a property inquiry from the resolved page.", "Link click and inquiry share the expected distribution source/medium/campaign attribution.", "click_count +1; PropertyEvent click + inquiry; Lead/interest attribution fields set without duplicate lead.", "GET short link redirects; inquiry POST returns created/reused response.", "Visitor reaches the correct listing and sees successful inquiry.", "Automation assignment and dashboard source metrics use the preserved attribution."),
            ("Agent portal export includes only assigned listings", "Permission", "P0", "Agent", "Request portal CSV with own and another agent's property IDs.", "Export contains only listings visible to the requesting agent; no tenant/assignment leak.", "No rows change.", "GET portal-export returns a downloadable CSV scoped to assigned properties.", "Downloaded file excludes unauthorized titles/addresses.", "No events are created merely by export."),
            ("Media package contains expected branded files", "Integration", "P1", "Agency Manager", "Download watermarked and complete packages and inspect ZIP entries.", "Package includes the documented source/derived media and copy files, with agency watermark/branding and safe filenames.", "No source media rows/files are modified.", "Asset endpoint returns correct content type, filename and non-empty bytes.", "Browser download succeeds; archive opens without corruption.", "Generated contents use current localization/property data."),
            ("Deleted or disabled short link no longer resolves", "State Transition", "P1", "Agency Manager/Public visitor", "Delete the link, then request its public code and submit no-cache requests.", "Code no longer redirects; other links remain valid.", "DistributionLink removed/disabled; no new click/event after deletion.", "Public code returns 404/gone according to current view.", "Visitor sees unavailable link rather than tenant detail.", "Analytics do not increment for rejected requests."),
        ],
    ),
    spec(
        "storefront", "10_Public_Storefront", "Public Storefront and Discovery", "PUB",
        "Resolve an agency storefront, browse/filter/map public listings, view safe property/agent details, similar properties, track events, contact the agency, and submit inquiries.",
        "Anonymous public visitor; Customer account for saved actions",
        "Next.js /agency/[agencySlug] and template routes; React public /agency/:slug routes",
        "GET public agency/agent/property/filter/similar endpoints; POST public contact/inquiry/event/site-visit endpoints",
        "Agency, Property, PropertyMedia, PropertyEvent, PublicSubmission, Lead, LeadPropertyInterest, LeadInteraction",
        "Published and unpublished agencies; eligible/ineligible properties; public agents; browser with JS/network inspection.",
        "Search/filter params, property IDs/share slugs, visitor ID, UTM/referrer, inquiry/contact payloads, invalid tenant/property combinations.",
        "Agency publishing, property freshness, localization, leads/automation, customer portal, analytics",
        "Open the agency site, filter properties, view a listing, inspect similar listings, and submit an inquiry.",
        "Only eligible tenant content appears; public payload is safe; view/inquiry events and CRM records are created with attribution.",
        "Reject inactive/unpaid/unpublished agency, draft/sold/rented/expired/unpublished property, cross-agency property ID, malformed inquiry and abusive request rate.",
        "Anonymous reads/actions only use public endpoints; no internal IDs/data beyond serialized public contracts; customer-only save actions require a customer token.",
        "Property filters and similar results stay within the resolved agency and public eligibility queryset.",
        "Public delete is unavailable; reports and inquiries add records without changing property ownership/status.",
        "Duplicate inquiry reuses lead/interest per current deduplication logic; view event endpoint may record every accepted event by design.",
        "Reads are unchanged; accepted inquiry adds/reuses Lead/Interest/Interaction and PropertyEvent; contact adds submission/lead as implemented.",
        "Lead automation, notifications, dashboard/event analytics and agent workload may change.",
        [
            ("Inactive agency properties are never public", "Security", "P0", "Public visitor", "Query list/detail/share slug after Agency is suspended or subscription becomes inactive.", "All public property access is excluded even when Property.is_published remains true.", "No rows change.", "Public endpoints return empty/404; existing authenticated JWT also fails entitlement check.", "Site shows unavailable/not found and no stale listing card.", "Search engines and short links cannot bypass the gate."),
            ("Duplicate public inquiry reuses lead and interest", "Idempotency", "P0", "Public visitor", "Submit the same normalized phone/email inquiry twice for one property.", "Current deduplication reuses the existing lead and property interest while retaining appropriate interaction/event evidence.", "Lead count stays 1; interest count stays 1; interaction/event behavior matches service implementation.", "Both POSTs succeed with created/reused semantics and no 500.", "Confirmation appears without duplicate visible CRM lead.", "Automation must not double-assign or double-alert unnecessarily."),
            ("Public filter options exclude private inventory", "Security", "P1", "Public visitor", "Create unique city/type values only on draft/expired properties, then fetch filter options.", "Private-only values are not leaked through filter facets.", "No rows change.", "GET public filter-options returns values from eligible queryset only.", "Filter controls omit the private values.", "No inference of hidden stock."),
            ("Property event validation and attribution", "API", "P1", "Public visitor", "Send valid view/call/WhatsApp/Viber events and an invalid event type with UTM/referrer.", "Valid events persist against the correct tenant property; invalid type is rejected.", "PropertyEvent +1 per valid accepted call; none for invalid/cross-tenant call.", "POST events returns success for choices and 400/404 for invalid scope/type.", "Navigation/click action continues even if noncritical analytics fails gracefully.", "Dashboard/event reports reflect accepted events."),
        ],
    ),
    spec(
        "leads", "11_Leads", "Lead CRM and Unified Lead Workspace", "LEAD",
        "Capture and manage demand from website, Meta, phone, walk-in, WhatsApp, Viber, referrals and portals with interests, interactions, documents, status history, follow-ups and a unified workspace.",
        "Agency Owner; Agency Manager; assigned Agent",
        "/leads and lead workspace/detail panels",
        "GET/POST /api/leads/; GET/PATCH/DELETE /api/leads/{id}/; interests, interactions, complete-follow-up, timeline, workspace and documents subroutes",
        "Lead, LeadPropertyInterest, LeadInteraction, LeadStatusHistory, LeadDocument, SiteVisit, Deal, Conversation",
        "Agency A/B users; assigned/unassigned leads; properties; interactions; visits; documents; due follow-ups.",
        "All explicit lead sources/statuses, contact identity, budgets, property interests, interaction directions, follow-up dates, lost reason.",
        "Public inquiries, automation, inbox, site visits, deals/offers, contacts, documents, tasks, dashboard",
        "Create a lead, assign it, add property interest and communication history, schedule/complete follow-up, and inspect workspace/timeline.",
        "Tenant-scoped lead aggregates all related context; status/follow-up/response timestamps and history update consistently.",
        "Reject missing identity, invalid source/status, lost without reason, cross-tenant property/agent/doc, agent access to another agent's workspace, and invalid follow-up date/state.",
        "Owner/manager sees agency leads; agent sees assigned leads/workspaces; assignment and related IDs must be tenant scoped.",
        "Filter/search/status/source/assigned agent endpoints return scoped results; workspace returns related features without duplication.",
        "Delete behavior cascades lead interests/interactions/history/documents and may be protected/referenced by operational records; owner/manager permission must be confirmed; test against actual on_delete graph.",
        "Public service deduplicates normalized identity; manual POST does not expose an idempotency key, so double-submit behavior must be recorded.",
        "Lead and related rows add/update/delete; status changes add LeadStatusHistory; outbound interaction updates last_contacted/first_response metrics.",
        "Assignment notifications/SLA, dashboard, contacts import, visits/deals/inbox links and automation events can change.",
        [
            ("Lost lead requires lost reason", "State Transition", "P0", "Agency user", "PATCH status to lost with blank reason, then with a reason.", "Blank reason is rejected; valid reason creates a status history entry and persists lost_reason.", "First attempt no change; second updates Lead and adds LeadStatusHistory.", "PATCH returns 400 then 200.", "Form blocks/marks the reason field and timeline shows accepted transition.", "Follow-up/automation handling follows lost status rules."),
            ("Outbound interaction records first response time", "Integration", "P0", "Assigned Agent", "Add the first outbound interaction after assignment, then another outbound interaction.", "First response duration is recorded once; last_contacted_at updates appropriately without overwriting the first metric.", "Lead response fields update; two LeadInteraction rows.", "POST interactions returns 201 for each valid entry.", "Workspace/timeline immediately shows both interactions and response KPI.", "Neglect escalation should no longer treat the lead as unresponded."),
            ("Agent cannot open another agent's workspace", "Security", "P0", "Agent A", "GET workspace/timeline/documents for Agent B's assigned lead.", "Access is hidden/forbidden across every sub-resource.", "No rows change.", "Scoped endpoints return 404/403 consistently.", "Direct URL shows forbidden/not found and no cached details.", "No side-effect event or notification."),
            ("Complete follow-up records next action", "Workflow", "P1", "Assigned Agent", "Complete a pending follow-up with outcome/notes and optional next_follow_up_at.", "Follow-up status/timestamps and interaction/history reflect completion; next reminder state is set only when requested.", "Lead updates and one LeadInteraction is created by the endpoint.", "POST complete-follow-up returns updated lead/context.", "Due item clears and timeline shows completion.", "Reminder job does not resend the completed reminder window."),
        ],
    ),
    spec(
        "lead_auto", "12_Lead_Automation", "Rule-Based Lead Automation", "AUTO",
        "Assign leads using listing agent, property, location, property type and round-robin rules with capacity, duplicate detection, SLA tracking, reminders, escalation, neglected-lead alerts and reassignment.",
        "Agency Owner; Agency Manager configure/review; Agent receives assignments/reminders; scheduler/system processes",
        "/leads automation settings/rules/dashboard/duplicates/events panels",
        "GET/PATCH /api/leads/automation/settings/; CRUD /api/leads/automation/rules/; duplicate review/events/dashboard; POST /api/leads/automation/process/",
        "LeadAutomationSettings, LeadAssignmentRule, LeadDuplicateFlag, LeadAutomationEvent, Lead, Notification",
        "Multiple active agents with capacities; listing agents; prioritized rules; duplicate leads; timestamps crossing SLA/reminder/escalation thresholds.",
        "Rule priority/conditions/assignee, max active leads, round-robin cursor, SLA minutes, reminder/escalation/reassignment windows.",
        "Lead creation, properties/agents, interactions, notifications/email, scheduler",
        "Configure prioritized rules, create matching leads, process automation, respond to one lead and leave another neglected.",
        "Deterministic assignment honors priority/capacity/fallback; SLA and events are recorded; reminders/escalation/reassignment occur once per eligible threshold.",
        "Reject agent configuration, cross-tenant assignees/property conditions, malformed rules/windows, inactive agents, and invalid duplicate review state.",
        "Only owner/manager configures rules/settings and reviews duplicates; agents can see only their assigned lead effects.",
        "Automation dashboard/events/duplicate queues are tenant-scoped and reflect actual processed state.",
        "Deleting a rule stops future matches but must not unassign existing leads; deleting leads cascades their automation flags/events per model relations.",
        "Processing command/API is designed to avoid duplicate reminder/escalation events per window; concurrent processors must be tested for races.",
        "Lead assigned_agent/SLA/reassignment fields update; automation event/duplicate/notification rows add; settings/rules persist.",
        "Agent workload, emails/in-app alerts, lead filters, response KPIs and manager dashboard change.",
        [
            ("Higher-priority location/type rule wins", "Workflow", "P0", "Agency Manager", "Create overlapping location and property-type rules with distinct priorities, then create a lead matching both.", "The highest-priority active rule assigns the configured eligible agent.", "Lead assigned_agent and assignment timestamps set; one assignment event.", "Lead create/process returns assigned result; automation dashboard increments.", "Lead card shows the winning agent/rule outcome.", "Assignment notification/SLA starts once."),
            ("Round robin respects maximum active capacity", "Boundary", "P0", "System", "Fill Agent A to max_active_leads, leave Agent B below capacity, and process several matching leads.", "Capacity-full agent is skipped; eligible agents rotate deterministically; no lead exceeds configured agent capacity due to sequential processing.", "Assignments and round-robin state update without cross-tenant candidates.", "Process endpoint/command succeeds and reports counts.", "Dashboard workload matches assignments.", "Notifications sent only to actual assignees."),
            ("Duplicate phone is flagged for manager review", "Integration", "P1", "Agency Manager", "Create two Agency A leads with the same normalized phone and then one in Agency B.", "Only the Agency A pair creates a tenant-scoped pending duplicate flag; no cross-agency comparison.", "One LeadDuplicateFlag for A pair; none across A/B.", "Duplicates list returns only requesting tenant; review endpoint accepts confirmed/dismissed.", "Manager sees the pair and reasons/score.", "Leads are not silently merged by review unless explicitly implemented."),
            ("Neglected lead escalation and reassignment are idempotent", "Background Job", "P0", "Scheduler", "Backdate an unanswered lead past escalation/reassignment thresholds and run processing twice.", "Manager alert and reassignment occur once for the eligible window; second run produces no duplicate side effects.", "One logical escalation/reassignment event; lead assignee/history timestamps stable after second run.", "Command/API reports first-run work then zero/already-processed.", "Dashboards show one escalation; users do not receive duplicate alerts.", "Old/new assignee notification behavior matches current service."),
        ],
    ),
    spec(
        "inbox", "13_Unified_Inbox", "Meta Unified Inbox", "INBOX",
        "Normalize Facebook and Instagram messages into tenant conversations, assign/claim them, reply, mark read, change status, and link/create CRM leads.",
        "Agency Owner; Agency Manager; assigned/claiming Agent; Meta webhook system",
        "/inbox",
        "GET /api/inbox/conversations/; detail/messages/reply/assign/link-lead/create-lead/mark-read/status; GET/POST /api/webhooks/meta/",
        "InboxConversation, InboxMessage, SocialAccount, Lead, LeadInteraction",
        "Connected Meta Facebook/Instagram accounts for two agencies; valid app secret/signature; inbound webhook fixtures; mocked Graph API.",
        "Provider conversation/message IDs, text/media message types, delivery statuses, assignments, lead IDs and conversation statuses.",
        "Meta social accounts, lead service/automation, agents, Graph API, webhook security",
        "Ingest a signed message, open/assign the conversation, create or link a lead, reply, mark read and close/pending it.",
        "Inbound provider message is stored once in the correct tenant conversation; CRM linkage and outbound delivery state are consistent.",
        "Reject invalid signature, unknown account, duplicate provider message, cross-tenant assignment/lead, unassigned-agent access where restricted, and provider reply errors.",
        "Conversations are tenant scoped; agents can claim/reply according to current view permissions; cross-agency conversations/accounts/leads are never linkable.",
        "Conversation list filters/search/status/assignment remain tenant-scoped; messages ordered chronologically.",
        "Conversation status close/spam does not delete message history; hard-delete endpoints are not exposed.",
        "Webhook delivery is idempotent on provider IDs; repeated reply action may send twice if client repeats after ambiguous timeout, so test and document.",
        "Conversation/message rows add/update; unread_count/last_message_at/status/assignment update; create-lead may add Lead/Interaction/automation records.",
        "Agent notifications, lead workspace/timeline, automation assignment, manager inbox metrics and Graph delivery change.",
        [
            ("Invalid Meta webhook signature is rejected", "Security", "P0", "External caller", "POST a realistic webhook body with missing/incorrect X-Hub-Signature-256.", "Request is rejected before any payload processing.", "No conversation/message/lead rows are created or changed.", "POST /api/webhooks/meta/ returns the implemented authentication error.", "No inbox update appears.", "No Graph API or notification call occurs."),
            ("Same provider message is ingested once", "Idempotency", "P0", "Meta webhook system", "Deliver the identical Facebook/Instagram message event twice.", "One message exists and conversation counters/timestamps are not doubled.", "InboxMessage unique provider ID prevents duplicate; unread_count increments once.", "Both deliveries are acknowledged safely without 500.", "One message bubble appears.", "At most one lead/notification side effect."),
            ("Facebook and Instagram normalize into same inbox contract", "Integration", "P1", "Agency user", "Ingest equivalent text/media payloads from both connected platforms.", "Both appear through the same conversation/message API with correct platform/account metadata.", "Separate provider records with normalized fields and correct agency.", "Conversation list/messages return 200 and common serializer shape.", "UI renders consistent bubbles and platform badges.", "CRM link/create actions work for either platform."),
            ("Reply provider failure preserves actionable local state", "Recovery", "P0", "Assigned Agent", "Mock Graph timeout/4xx while sending a reply.", "User gets an actionable error and delivery is not falsely marked sent; safe retry remains possible.", "Outbound message is failed/pending according to implementation with error_message; inbound history unchanged.", "Reply endpoint returns non-2xx/actionable payload.", "Failed status and retry guidance appear; no duplicate success bubble.", "No lead response-time success should be recorded until send succeeds."),
        ],
    ),
    spec(
        "visits", "14_Site_Visits", "Site Visits", "VISIT",
        "Create and manage property viewing requests, schedule reminders, enforce cancellation reasons, and synchronize lead progress/history.",
        "Public visitor; Agency Owner; Agency Manager; assigned Agent; scheduler",
        "/site-visits and public property visit form",
        "GET/POST /api/site-visits/; GET/PATCH/DELETE /api/site-visits/{id}/; POST /api/public/agencies/{license}/properties/{property_id}/request-site-visit/",
        "SiteVisit, Lead, LeadStatusHistory, LeadInteraction, Notification, PropertyEvent",
        "Public eligible property; lead/agent fixtures; future/past visit dates; reminder email capture; two agencies.",
        "Visitor identity/contact, scheduled_at, status requested/scheduled/completed/cancelled, notes and cancellation_reason.",
        "Public property, lead dedup/automation, email/reminders, dashboard and deal follow-up",
        "Request a public visit, schedule it, send reminder, complete it, and inspect lead timeline.",
        "Visit is tenant/property scoped; lead is created/reused; completion advances lead/history; cancellation requires reason; reminder is sent once.",
        "Reject invalid/cross-agency property or agent, past/invalid dates where validated, completion without permission, cancellation without reason, and invalid status choice.",
        "Agency users see tenant visits; agents only assigned/relevant visits according to queryset; public can only create for eligible listing.",
        "Filter/order by status/date/agent/property as exposed; direct detail tenant scoped.",
        "Delete is authenticated and permission-scoped; deleting a visit must not delete lead/property; repeated delete returns not found.",
        "Public duplicate identity reuses lead; reminder command is idempotent; duplicate visit requests may create distinct visits unless guarded—record current behavior.",
        "SiteVisit +1/update/delete; public request adds/reuses Lead/Interest/Interaction/Event; completion adds LeadStatusHistory; reminder timestamp/error updates.",
        "Lead status/workspace, agent notifications/email, dashboard and source attribution change.",
        [
            ("Completion updates lead and status history", "Integration", "P0", "Assigned Agent", "Mark a scheduled visit completed for a lead not already beyond the visit stage.", "Visit completes and lead advances to the implemented status with history.", "SiteVisit status/completed fields update; Lead and LeadStatusHistory update once.", "PATCH detail returns 200 with completed status.", "Visit and lead views reflect completion immediately.", "Automation/deal follow-up can use the new lead state."),
            ("Cancellation requires reason", "Validation", "P0", "Agency user", "PATCH status=cancelled with blank reason, then with a meaningful reason.", "Blank request fails; valid request cancels and retains reason.", "First attempt unchanged; second updates SiteVisit.", "PATCH returns 400 then 200.", "Cancellation modal requires/displays reason.", "Lead is not incorrectly marked completed."),
            ("Reminder command is idempotent", "Background Job", "P0", "Scheduler", "Place a scheduled visit in reminder window and run send_due_reminders twice.", "One reminder is sent and the second run skips it.", "reminder_sent_at set once; no duplicate notification/email record.", "Management command exits 0 both times with first/second counts.", "Agent/customer receives one reminder.", "Mail error field remains blank on success."),
            ("Public request rejects cross-agency property", "Security", "P0", "Public visitor", "Use Agency A license with Agency B property ID.", "Request is not created and property existence is not leaked beyond not-found semantics.", "No SiteVisit/Lead/Interest/Event in either tenant.", "POST returns 404/validation error.", "Safe not-found message; form remains usable.", "No notification/email/automation."),
        ],
    ),
    spec(
        "contacts", "15_Contacts_Owners", "Contacts and Property Owners", "CONT",
        "Maintain tenant CRM contacts and owners, link them to leads/properties, and import eligible leads into contacts while restricting agents to relevant records.",
        "Agency Owner; Agency Manager; assigned Agent",
        "/contacts and /owners",
        "CRUD /api/operations/contacts/; POST /api/operations/contacts/import-leads/; CRUD /api/operations/owners/",
        "Contact, Owner, Lead, Property, AuditLog",
        "Agency A/B; leads with/without contacts; assigned/unassigned agents; owners linked to one or many properties.",
        "Buyer/seller/tenant/landlord/investor/vendor types, names, email/phone, assigned_to, lead link, owner properties.",
        "Leads, properties, deals, leases, documents, audit logs",
        "Create a contact and owner, link owner properties, then import unconverted leads.",
        "Records are tenant scoped; agents see assigned/relevant contacts and owners of their assigned properties; import creates one contact per eligible lead.",
        "Reject cross-tenant lead/property/assigned user, invalid contact type, missing required names, duplicate unique relationships where constrained, and agent deletion.",
        "Agents are restricted by assigned_to/lead assignment and owner property assignment; only owner/manager deletes operations records.",
        "Search fields and import operate only within current tenant/agent scope.",
        "Contact/Owner deletion follows PROTECT/SET_NULL/CASCADE relationships; dependencies such as leases may block deletion. Failed delete must be atomic.",
        "import-leads excludes leads already linked to a Contact, so repeated import returns zero for processed leads.",
        "Contact/Owner and M2M/property links update; AuditLog entries on mutations; no lead deletion.",
        "Deal/lease/document selectors, owner property detail and reports change.",
        [
            ("Lead import is idempotent", "Idempotency", "P1", "Agency Manager", "Run contacts/import-leads twice for the same unconverted tenant leads.", "First run creates contacts; second reports zero new contacts and no duplicates.", "One Contact per Lead; lead contact reverse relation remains unique.", "POST returns created count then 0.", "Contacts list adds entries once.", "Audit behavior matches per-contact implementation; no notifications expected."),
            ("Agent sees only assigned contacts", "Permission", "P0", "Agent A", "List/search/detail Contact assigned to Agent A, Agent B, and unassigned unrelated lead.", "Agent A receives only records assigned to them or linked to their assigned leads.", "No rows change.", "List excludes unauthorized; detail returns scoped 404.", "UI count/search never reveals hidden contacts.", "Reports/selectors use the same restriction."),
            ("Owner deletion with protected lease dependency", "Database", "P1", "Agency Manager", "Attempt to delete an Owner referenced by Lease.owner and inspect configured SET_NULL behavior.", "Current model uses SET_NULL for Lease.owner, so owner deletes while lease remains with null owner.", "Owner removed; Lease row retained and owner_id null; AuditLog records deletion.", "DELETE returns 204 if no other blocker.", "Owner disappears; lease remains usable and shows no owner.", "No property/contact tenant ownership changes."),
        ],
    ),
    spec(
        "deals", "16_Deals_Offers", "Deals, Pipelines and Offers", "DEAL",
        "Track negotiations through agency/custom pipeline stages, assign deals, submit/respond to offers, and update the deal when an offer is accepted.",
        "Agency Owner; Agency Manager; assigned Agent",
        "/deals, /offers, /customization pipeline stages",
        "CRUD /api/operations/deals/; CRUD /api/operations/offers/; POST /api/operations/offers/{id}/respond/; CRUD /api/operations/pipeline-stages/",
        "Deal, Offer, PipelineStage, Lead, Contact, Property, Notification, AuditLog",
        "Agency A/B leads/contacts/properties/agents; default and custom pipeline stages; draft/submitted/countered offers.",
        "Deal title/stage/value, assigned_agent and related IDs; offer amount/currency/status/terms/expiry; response states accepted/rejected/countered/withdrawn.",
        "Leads, contacts, properties, tasks/documents, reports, notifications",
        "Open a deal from a lead/property, submit an offer, counter it, then accept a valid offer.",
        "Deal remains tenant scoped; accepted offer stamps responded_at and sets deal value to offer amount and stage to contract.",
        "Reject cross-tenant relationships, invalid/custom stage not configured, invalid response state, negative/oversized amount, expired offer behavior where enforced, and agent access to others' deals.",
        "Agent sees deals assigned directly or through assigned lead and offers on their deals; only owner/manager deletes operations records.",
        "Stage and deal filters/search preserve tenant/agent scope.",
        "Deal deletion cascades offers; protected/linked documents/tasks follow their on_delete behavior. Offer repeated delete returns 404.",
        "respond endpoint has no idempotency key; repeated accepted response should be observed but must not create duplicate offer/deal rows.",
        "Deal + Offer rows and AuditLog; accepted response updates Offer and Deal atomically enough to avoid inconsistent stage/value.",
        "Assignment/new-offer notifications, reports/dashboard, lead workspace and tasks/documents change.",
        [
            ("Accepted offer updates deal value and contract stage", "State Transition", "P0", "Assigned Agent/Manager", "POST respond status=accepted for a submitted offer.", "Offer becomes accepted with responded_at; Deal.value equals Offer.amount and stage becomes contract.", "One Offer update, one Deal update, audit entry; no new offer.", "POST /api/operations/offers/{id}/respond/ returns 200.", "Deal pipeline and offer badge update together.", "Reports reflect contract-stage value."),
            ("Invalid offer response state is rejected", "Validation", "P0", "Agency user", "Respond with draft/submitted/expired/arbitrary status.", "Only accepted, rejected, countered or withdrawn are accepted by respond action.", "Offer/deal/audit remain unchanged.", "POST respond returns 400 with explicit allowed choices.", "Modal keeps the offer state and displays error.", "No notification/report change."),
            ("Custom pipeline stage is accepted only for tenant", "Integration", "P1", "Agency Manager", "Create custom stage for Agency A, use it on Deal A, then try its identifier/value on Agency B.", "A accepts its configured stage; B cannot use A's custom definition.", "Deal A stage updates; Deal B unchanged; stage records remain tenant scoped.", "A request 200; cross-tenant request 400/404.", "Each pipeline shows only its agency stages.", "Reports group stages without tenant leakage."),
            ("Concurrent offer responses do not leave inconsistent deal", "Concurrency", "P0", "Two agency users", "Respond accepted and rejected/countered to the same offer concurrently.", "Current last-write behavior is captured; invariant requires Offer status and Deal stage/value to remain logically consistent. Optimistic locking is not implemented and is a risk.", "Inspect final Offer/Deal/Audit rows for split-brain outcome.", "Both responses may return success under current code; any inconsistent final state is a defect.", "UI refresh shows server final state and warns on conflict if implemented.", "Reports/notifications must match final accepted state only."),
        ],
    ),
    spec(
        "leases", "17_Leases_Docs", "Leases and Operational Documents", "LEASE",
        "Record rental lease terms and attach agency documents to leads, properties, deals, contacts or owners. This does not process rent payments.",
        "Agency Owner; Agency Manager; assigned Agent",
        "/leases and /documents",
        "CRUD /api/operations/leases/; CRUD /api/operations/documents/; POST/GET /api/leads/{id}/documents/",
        "Lease, Document, LeadDocument, Property, Contact, Owner, Deal, AuditLog",
        "Rental property; tenant Contact; optional Owner; assigned agents; files; date/status fixtures; no property transaction payment setup.",
        "Lease draft/active/expired/terminated/renewed, monthly rent, deposit, NPR, start/end dates; document metadata/file and relationship IDs.",
        "Properties, contacts/owners, deals/leads, file storage, tasks/reports",
        "Create a lease, activate it, attach related documents, then terminate/renew according to stored status choices.",
        "Lease and documents remain tenant-scoped and agent-restricted; rent values are records only and no payment transaction is initiated.",
        "Reject cross-tenant property/contact/owner/deal/lead, invalid dates/status/amount, unsafe file, missing required relationship/title, and agent deletion.",
        "Agents see assigned leases and documents they uploaded or that relate to assigned lead/deal/property; owner/manager deletion only.",
        "Search/filter lists honor scoped related fields and status.",
        "Property/tenant Contact are PROTECT for Lease deletion dependencies; deleting Lease removes only lease; Document deletion removes its row/storage object by configured storage behavior.",
        "Repeated upload creates multiple documents unless a uniqueness rule exists; current implementation has no general idempotency key.",
        "Lease/Document/LeadDocument/AuditLog rows; related Property/Contact remain unchanged; file stored for accepted upload.",
        "Lease reports/calendar/tasks may change; no bank/cash/rent payment side effect exists.",
        [
            ("Lease end date precedes start date", "Validation", "P0", "Agency user", "Create/update a lease with end_date before start_date and with negative rent/deposit.", "Serializer/model validation should return field errors; if accepted, flag as probable validation gap rather than assuming rejection.", "No invalid row should commit under expected rule; record actual current behavior.", "Expected/recommended 400; current implementation must be recorded if 201/200.", "Form should block dates/amounts or show server errors.", "No property status/payment/report mutation."),
            ("Deleting property referenced by lease is protected", "Database", "P0", "Agency Owner", "Attempt Property delete while Lease.property references it.", "PROTECT prevents property deletion and preserves the lease.", "Property and Lease remain; no partial media/history cascade; failed audit semantics inspected.", "DELETE property returns conflict/validation error rather than 500.", "User sees dependency explanation.", "Public listing remains until separately changed."),
            ("No rent payment is created from lease", "Regression", "P0", "Agency user", "Create/activate/renew a lease and inspect API calls/database.", "Only lease records change; no Stripe checkout, payment, transaction, escrow or bank instruction is created.", "Lease/Audit only; SubscriptionPayment remains untouched.", "No property-payment endpoint is called.", "UI shows recorded monthly rent/deposit but no pay action.", "SaaS billing remains separate."),
            ("Cross-linked document obeys every tenant relationship", "Security", "P0", "Agency A user", "Upload a Document with Agency A lead plus Agency B property/deal/contact IDs.", "Request fails; it cannot create a record with mixed-tenant references.", "No Document/file/Audit row created.", "POST returns 400/scoped not-found.", "Upload form shows relation error and no list entry.", "No private filename/metadata leak."),
        ],
    ),
    spec(
        "tasks", "18_Tasks_Notifications", "Tasks, Notifications and Audit Log", "TASK",
        "Assign operational tasks, track due/status/priority, notify assignees, send reminders, mark notifications read, and preserve manager-visible audit history.",
        "Agency Owner; Agency Manager; Agent for own tasks/notifications; Super Admin where scoped",
        "/tasks, /notifications, /audit-log, /calendar",
        "CRUD /api/operations/tasks/; read/read-all notification actions; GET /api/operations/audit-logs/",
        "Task, Notification, AuditLog, Lead, Deal, Property",
        "Agency A/B users; assigned tasks in past/today/future; linked CRM records; unread notifications.",
        "Task title/status todo/in_progress/done/cancelled, priority, due_at, assigned_to and related IDs.",
        "All operations mutations, email/reminder command, calendar, dashboard",
        "Create and assign a linked task, process its reminder, complete it, and inspect notification/audit records.",
        "Task is scoped and assigned correctly; assignee is notified; due filters/reminder/status/completed_at are consistent; audit log is manager-only/read-only.",
        "Reject cross-tenant assignee/relations, missing title, invalid status/priority/date, agent assignment to another user, agent delete, audit mutation and notification IDOR.",
        "Agent sees tasks assigned to or created by them and only their notifications; owner/manager sees tenant tasks/audit; audit is read-only.",
        "Task due filters overdue/today/upcoming and audit entity_type filter preserve scope.",
        "Only owner/manager deletes Task; Notification/audit delete endpoints are not exposed; related business records survive task deletion.",
        "Reminder processing and read-all are idempotent; assigning the same task twice must not duplicate assignment notification unless an actual reassignment occurs.",
        "Task/Notification/AuditLog update/add; read action flips is_read; completion should stamp completed_at according to serializer behavior.",
        "Calendar, dashboard counts, agent workload and email/in-app alerts change.",
        [
            ("Task assignment creates one notification", "Integration", "P0", "Agency Manager", "Create a task assigned to Agent A, then retrieve Agent A notifications.", "One task assignment notification links to /tasks and belongs only to Agent A.", "Task +1, Notification +1, AuditLog +1.", "POST task 201; notifications list 200 for Agent A and excludes it for others.", "Agent sees one unread notification and task.", "Calendar/dashboard may include the due task."),
            ("Notification IDOR is blocked", "Security", "P0", "Agent A", "Call read action using Agent B notification ID.", "Agent A cannot retrieve or mark it read.", "Agent B Notification.is_read remains false.", "POST notification/{id}/read returns scoped 404.", "No hidden title/message appears.", "No audit/side effect."),
            ("Due reminder command handles mail failure", "Recovery", "P1", "Scheduler", "Mock email failure for a due task and run send_task_reminders, then recover and rerun.", "Failure is logged/recorded without aborting unrelated tasks; retry behavior follows implementation and does not falsely mark success.", "Inspect reminder/error timestamps for failed and successful records.", "Management command exits/continues according to implementation; no API response.", "No false success notification; eventual retry reaches recipient.", "Other due tasks process independently."),
            ("Audit log cannot be modified", "Security", "P0", "Agency Owner", "Try POST/PATCH/DELETE against audit-log routes and access Agency B log ID.", "Mutations are method-not-allowed; cross-tenant reads are hidden.", "AuditLog rows are immutable through API.", "405 for mutations; 404/empty for tenant mismatch.", "Audit page offers no edit/delete control.", "Compliance history remains intact."),
        ],
    ),
    spec(
        "appointments", "19_Appointments", "Availability and Appointments", "APPT",
        "Publish agent availability, let customers request appointment slots, notify agents, and manage requested/confirmed/completed/cancelled appointments.",
        "Public customer; Agency Owner; Agency Manager; Agent",
        "/availability, /appointments, public customer portal",
        "CRUD /api/operations/availability/; CRUD /api/operations/appointments/; GET/POST /api/public/agencies/{slug}/appointments/",
        "AppointmentAvailability, Appointment, CustomerProfile, Property, AgencyUser, Notification",
        "Published agency; customer token; Agent A/B across tenants; availability slots; overlapping appointment fixtures.",
        "Weekday/start/end/active availability; appointment identity, agent/property, starts_at/ends_at, status and notes.",
        "Customer authentication, properties/agents, notifications, calendar",
        "Agent creates availability; customer requests an appointment; agency confirms/completes it.",
        "Public slots expose eligible agent availability; accepted request is tenant-scoped and notifies the correct agent; lifecycle persists.",
        "Reject cross-agency agent/property/customer token, invalid/overlapping time, end before start, unavailable slot if enforced, past appointment, duplicate unique availability start.",
        "Agent manages/sees own availability and appointments; owner/manager tenant-wide; customer only public endpoints with own token.",
        "Availability and appointment lists are scoped and chronological.",
        "Availability delete removes only slot definition; Appointment delete requires manager role under base viewset and must not delete customer/property/agent.",
        "Unique (agent, weekday, start_time) prevents duplicate availability start; appointment double booking prevention is not clearly enforced and requires clarification/testing.",
        "Availability/Appointment + rows; Notification +1 for public request; no property/lead row is automatically required by Appointment model.",
        "Agent calendar, customer portal and notifications change.",
        [
            ("Public appointment rejects cross-agency agent", "Security", "P0", "Public customer A", "POST Agency A appointment using Agent B ID and/or Property B ID.", "Request is rejected and no cross-tenant relationship is stored.", "No Appointment or Notification rows.", "POST returns 400/404.", "Portal shows safe validation error.", "Agent B receives nothing."),
            ("Duplicate availability start is constrained", "Concurrency", "P1", "Agent", "Send two concurrent creates for same agent/weekday/start_time.", "At most one row commits due to unique constraint; second request returns validation/integrity-safe error.", "Exactly one AppointmentAvailability row.", "One 201 and one 400/conflict, never 500.", "Schedule displays one slot.", "No duplicate public slot."),
            ("Concurrent appointment requests for same time", "Concurrency", "P0", "Two customers", "Submit two requests for Agent A at identical starts_at/ends_at.", "Current model has no visible uniqueness constraint; capture whether both are accepted. Product-level no-double-booking expectation requires clarification.", "Inspect Appointment count and final states.", "Record actual 201/validation behavior; flag duplicate acceptance as risk, not invented failure.", "Portal must show accurate outcome to each customer.", "Agent notifications must match accepted records."),
            ("Nepal timezone boundary renders consistently", "Boundary", "P1", "Customer/Agent", "Book around midnight, DST-irrelevant Nepal offset, month/year boundary and BS/AD display switch.", "Stored UTC/API time and Nepal-local UI time represent the same instant without date shift.", "Datetime values persist once; no duplicate appointment.", "ISO timestamps round-trip correctly.", "Calendar/portal shows Asia/Kathmandu local date/time and selected date mode.", "Reminder scheduling uses the same instant."),
        ],
    ),
    spec(
        "customer", "20_Customer_Portal", "Public Customer Accounts, Saved Properties and Searches", "CUST",
        "Register/login customers per agency, persist a storefront session token, save eligible properties/searches, receive scheduled search alerts, and request appointments.",
        "Public customer account; Agency staff see related operational records where exposed",
        "Public /agency/:slug/portal and Next.js customer portal",
        "POST customer register/login; GET/POST/DELETE saved-properties; GET/POST saved-searches; GET/POST public appointments",
        "CustomerProfile, SavedProperty, SavedSearch, Appointment, Property, Notification/email outputs",
        "Published Agency A/B; customer accounts; public properties; saved-search criteria; browser localStorage; scheduler/email capture.",
        "Customer email/password/name/phone, tenant token, property ID, filter JSON and alert cadence.",
        "Public website, property eligibility, appointments, saved-search scheduler/email",
        "Register for Agency A, login, save/unsave a property, create a saved search, and request an appointment.",
        "Customer token is agency-scoped; saved items reference only public Agency A inventory; repeated toggles are consistent; alerts use current matches.",
        "Reject unpublished agency, bad credentials, weak password, duplicate identity conflicts, wrong-agency token/property, private property, malformed filter JSON and unauthenticated save.",
        "Customer token grants only that customer/agency portal operations and never agency staff APIs or another customer's data.",
        "Saved lists/searches are owner and tenant scoped; available appointments are public but create uses authenticated customer header.",
        "Saved-property DELETE/toggle removes only the join row; customer hard-delete API is not exposed; property deletion cascades/cleans saved references according to model.",
        "Unique customer email/tenant and saved property relationship prevent duplicates; toggle intentionally adds/removes; alert command must dedupe by last_sent/match window.",
        "CustomerProfile/session token fields, SavedProperty/SavedSearch/Appointment rows; no Lead automatically unless appointment flow explicitly creates one.",
        "Storefront heart/portal, search-alert emails and agent appointment notification change.",
        [
            ("Customer token cannot cross agency", "Security", "P0", "Customer A", "Use Agency A token against Agency B saved-properties/searches/appointments endpoints.", "Every request is unauthorized/not found and reveals no B data.", "No rows change in either agency.", "Returns 401/403 according to customer authentication helper.", "Portal clears/rejects mismatched session without showing data.", "No emails/notifications."),
            ("Saved property toggle is duplicate safe", "Idempotency", "P1", "Customer", "Double-click save concurrently, refresh, then unsave twice.", "At most one saved relationship exists; final toggle/removal behavior is deterministic and no server error occurs.", "Unique SavedProperty relation count is 0 or 1 as expected after each phase.", "POST/DELETE responses match current toggle API and handle repeat safely.", "Heart and portal list converge after refresh.", "No property/lead changes."),
            ("Unpublished website rejects registration", "Security", "P0", "Anonymous visitor", "POST registration for agency with public website disabled.", "Registration is rejected and no customer token is issued.", "No CustomerProfile.", "POST returns not found/forbidden according to public agency queryset.", "Portal shows unavailable.", "No welcome email/appointment ability."),
            ("Saved-search alerts are idempotent and public-only", "Background Job", "P0", "Scheduler", "Create a search that matches one public and one expired/private property; run alerts twice in one cadence window.", "Only public match is included and one alert is sent for the window.", "SavedSearch last-sent metadata updates once; no saved property auto-created.", "Command exits successfully with deduped counts.", "Customer receives one correct localized email.", "Private listing data is never disclosed."),
        ],
    ),
    spec(
        "website", "21_Website_Content", "Website Content, Submissions and Agent Reviews", "WEB",
        "Manage agency website submissions and moderated agent reviews while converting valuable public forms into CRM leads where implemented.",
        "Public visitor; Agency Owner; Agency Manager; Agent for own reviews/submissions",
        "/website-content, /website-submissions, /agent-reviews and public forms",
        "POST public submissions/reviews; CRUD /api/operations/website-submissions/ and /agent-reviews/",
        "PublicSubmission, AgentReview, Lead, LeadInteraction, AuditLog",
        "Published agency; public agents; submission kinds valuation/contact/newsletter/career/demo/listing_report; moderation users.",
        "Form identity/message/property/agent, kind/status, review rating/title/comment, approval fields.",
        "Public storefront, leads, agents, notifications, reports",
        "Submit a valuation/contact/review, process the submission internally, and approve a valid review.",
        "Submission is tenant-scoped; lead creation/dedup follows kind-specific logic; review is hidden until manager approval.",
        "Reject unpublished agency, cross-agency agent/property, invalid kind/status/rating, spam/unsafe text, agent self-approval, and malformed/blank identity per form rules.",
        "Public creates only; agent sees own assigned submissions/reviews; only owner/manager moderates is_approved.",
        "Internal search/kind/status filters preserve tenant scope; public review list exposes approved only.",
        "Owner/manager delete operations records; public cannot delete; lead remains according to relationship on_delete.",
        "Newsletter submissions with blank phone must not accidentally merge unrelated leads; public rate throttle limits repeated abuse.",
        "PublicSubmission/AgentReview +1; valuation may add/reuse Lead/Interaction; moderation stamps approved_by/approved_at; audit on internal mutation.",
        "Lead inbox/dashboard, public agent reputation and submission queues change.",
        [
            ("Valuation submission creates CRM lead", "Integration", "P0", "Public visitor", "Submit valid valuation form with contact and property context.", "Submission and tenant CRM lead/interaction are created or safely reused according to lead service.", "PublicSubmission +1; Lead +1/reused; LeadInteraction +1; link fields set.", "POST returns created response.", "Public confirmation; manager sees submission and lead.", "Automation assignment/notification may run."),
            ("Newsletter blank phone does not merge strangers", "Edge Case", "P0", "Two public visitors", "Submit two newsletter forms with different emails and blank phone.", "They are not merged solely because normalized blank phone values match.", "Distinct submissions; lead behavior follows kind logic without a false shared lead.", "Both POSTs succeed independently.", "Each sees confirmation.", "No cross-person timeline contamination."),
            ("Agent review requires moderation", "State Transition", "P1", "Public visitor/Manager", "Submit review, verify public absence, approve as manager, verify public presence, then unapprove.", "Only approved review is publicly visible; approval fields stamp/clear correctly.", "AgentReview updates approved_by/approved_at; audit entries.", "Public POST created; internal PATCH 200; public agent payload changes.", "Review appears/disappears after moderation.", "Agent rating/reputation summaries update accordingly."),
            ("Agent cannot approve own review", "Permission", "P0", "Agent", "PATCH is_approved on a review for themselves.", "Request is forbidden by manager-role check.", "Approval fields unchanged; no audit update.", "PATCH returns 403.", "No moderation control; direct request error.", "Public visibility unchanged."),
        ],
    ),
    spec(
        "social", "22_Social_Media", "Meta Connections and Social Publishing", "SOC",
        "Connect agency Facebook/Instagram accounts, create/schedule/publish posts, track per-target outcomes, and safely edit/delete supported published media.",
        "Agency Owner; Agency Manager; authorized agency user according to view permissions; scheduler; Meta OAuth/Graph",
        "/social-media and distribution social-draft handoff",
        "CRUD /api/social-posts/posts/; POST publish; OAuth start/callback; GET accounts; POST/DELETE disconnect action",
        "SocialPost, SocialAccount, SocialPostTarget, PropertyMedia",
        "Meta app credentials; mock Graph API; Facebook Page and Instagram Business account; HTTPS-public media; scheduled times.",
        "Caption, target_platforms list, account IDs, media URL/file, draft/scheduled/published/partial/failed, OAuth code/state.",
        "Distribution toolkit, property media, scheduler, Meta Graph API, public media origin",
        "Connect Meta accounts, create a Facebook/Instagram draft, publish/schedule, retry partial failure, then edit/delete supported content.",
        "Accounts are tenant-scoped; platform targets dedupe; per-target results drive post state; retries target only failed destinations.",
        "Reject invalid OAuth state/code, another tenant's account, malformed target list, unsupported Instagram edit, non-HTTPS media, timeout/rate/auth failures.",
        "Only current agency accounts/posts are selectable; direct cross-tenant IDs are rejected.",
        "Post/account lists and status filters remain tenant scoped.",
        "Disconnect marks/removes connection as implemented; delete published remote media before local row and keep local post when provider deletion fails.",
        "Publish retries skip already successful targets; webhook/OAuth callback state must be replay-safe; repeated manual publish must not duplicate successful remote posts.",
        "SocialAccount/Post/Target state updates; provider IDs/errors stored; local caption updates only after supported remote success.",
        "Meta content changes, distribution campaign tracking and scheduler status change.",
        [
            ("Partial failure retries only failed target", "Recovery", "P0", "Agency user", "Publish to Facebook and Instagram with Instagram mocked to fail, then recover and publish again.", "First state is partial; retry does not duplicate Facebook and publishes only failed Instagram target.", "Target states/provider IDs persist independently; final post becomes published.", "Publish returns partial then success/actionable target results.", "Per-platform badges/errors update accurately.", "Exactly one remote post per platform."),
            ("Another agency social account cannot be selected", "Security", "P0", "Agency A user", "Create/update/publish using Agency B SocialAccount ID.", "Serializer/view rejects account and no provider call occurs.", "No SocialPost/Target mutation referencing B.", "POST/PATCH returns 400/404.", "B account is absent from selector; tampered request errors.", "No external Meta side effect."),
            ("Instagram requires HTTPS public media", "Validation", "P0", "Agency user", "Publish Instagram target with localhost/http/private media URL.", "Publish fails with actionable public-origin requirement and remains retryable.", "Target failed with error; no external_post_id.", "Publish response reports target failure without server crash.", "UI explains HTTPS/public requirement.", "Facebook target may still succeed, yielding partial state."),
            ("Published Instagram edit is rejected without local drift", "External Integration", "P0", "Agency user", "Try changing caption or replacing image on a published Instagram target.", "Unsupported remote edit is rejected and local caption/media remain unchanged.", "SocialPost and target unchanged.", "PATCH returns validation error.", "Editor keeps server state and explains limitation.", "No Meta API mutation."),
            ("Provider timeout and rate limit recovery", "Recovery", "P1", "Agency user/Scheduler", "Mock Graph timeout, 429 and expired token for separate publish attempts.", "Each produces actionable failed target state; successful targets stay intact; reconnect/retry path is available.", "Errors stored without false provider IDs.", "API returns partial/failed response; scheduler continues other posts.", "Status/error/reconnect controls are clear.", "No duplicate remote post on safe retry."),
        ],
    ),
    spec(
        "reports", "23_Reports_Matching", "Dashboard, Reports, Matching and Comparison", "RPT",
        "Summarize tenant operations, score property matches for a lead, compare selected properties, and expose platform/agency dashboard metrics.",
        "Agency Owner; Agency Manager; assigned Agent for scoped data; Super Admin for platform summary",
        "/dashboard, /analytics, /matching, /compare, /platform-admin",
        "GET /api/dashboard/summary/; GET /api/operations/reports/summary/; GET /api/operations/matching/leads/{id}/; GET /api/operations/properties/compare/; GET /api/operations/admin/summary/",
        "Aggregates across Lead, Property, Deal, Task, Appointment, PropertyEvent, Agency",
        "Two tenants with known counts/values; leads with criteria; public/private properties; agent-assigned subsets.",
        "Date/status/source filters, lead ID, selected property IDs, empty and large datasets.",
        "Nearly every operational feature; tenant scoping and property canonical data",
        "Create known business records, load summaries, match a lead, and compare allowed properties.",
        "Counts/totals/scores reconcile with database and role scope; no hidden tenant/private inventory leaks.",
        "Reject cross-tenant lead/property IDs, invalid/missing IDs/filters, agent requests for unauthorized data, and huge selection abuse.",
        "Agency reports scoped to tenant/agent rules where implemented; platform admin only super admin.",
        "Filter periods/statuses produce deterministic calculations including zero data.",
        "Read-only endpoints; no source records should change.",
        "Repeated calls return consistent values for unchanged data; caching/staleness behavior is not determined from current implementation.",
        "None for pure reads; any analytics event endpoint is separate.",
        "Dashboard cards, charts, matching order and comparison table update only.",
        [
            ("Report totals reconcile to tenant database", "Database", "P0", "Agency Owner", "Seed known properties/leads/deals/tasks/events and compare report JSON to SQL counts/sums.", "Every metric equals the documented queryset result for Agency A and excludes Agency B.", "No rows change.", "GET reports/summary returns 200 with numeric values.", "Charts/cards match API exactly, including zero states.", "No notification/audit side effect."),
            ("Matching ranks relevant public/tenant property", "Functional", "P1", "Agency user", "Create lead criteria and candidate properties varying type/location/price/area; request matches.", "Relevant allowed property receives higher score with explainable component values.", "No rows change.", "GET matching/leads/{id}/ returns ordered scored results.", "Matching UI order and reasons match response.", "Selecting a result can feed comparison without mutation."),
            ("Compare rejects cross-tenant/private property IDs", "Security", "P0", "Agency A user", "Request comparison with Agency A and Agency B property IDs.", "Only authorized scoped properties are returned or request is rejected without B data.", "No rows change.", "GET properties/compare returns scoped response/404 validation.", "Comparison table never shows B title/address/price.", "No event or history."),
            ("Platform admin summary is super-admin only", "Permission", "P0", "Agency Owner and Super Admin", "Call admin summary and platform-agencies list as both roles.", "Owner receives 403; super admin receives cross-agency platform metrics.", "No rows change.", "403 then 200.", "Platform admin navigation/content hidden from owner.", "No tenant record mutation."),
        ],
    ),
    spec(
        "billing", "24_Subscriptions", "Agency SaaS Subscription Billing", "BILL",
        "Gate RealtyOS access using agency SaaS subscription/payment state and integrate Stripe Checkout, billing portal and webhooks. This is not property-sale or rent collection.",
        "Agency Owner; Super Admin; Stripe webhook system; authenticated agency users affected by entitlement",
        "/billing, /payment-required",
        "GET /api/operations/subscriptions/; POST checkout; POST billing-portal; GET subscription-plans; POST /api/webhooks/stripe/; DEBUG-only mark-paid endpoint",
        "SubscriptionPlan, Subscription, SubscriptionPayment, Agency payment fields",
        "Stripe test credentials/webhook secret or mocks; trialing/active/past_due/cancelled/expired agencies; test plans.",
        "Plan code, Stripe session/customer/subscription/event IDs, valid/invalid signatures, duplicated/out-of-order events.",
        "Authentication gate, Stripe API/webhooks, platform admin, agency access",
        "Start Checkout for a plan, deliver signed webhook events, verify entitlement, and open billing portal.",
        "Subscription/customer/payment state maps to the correct agency; valid active entitlement permits JWT use; expired state rejects ordinary users.",
        "Reject inactive/unknown plan, missing Stripe config, invalid signature, unknown agency metadata, duplicate/out-of-order callbacks and provider failures safely.",
        "Agency reads its own subscription; webhook authenticates by signature; super admin platform controls; no property/payment transaction rights are introduced.",
        "Subscription list is agency scoped; plans expose active catalog only.",
        "Subscription endpoint is read-only except checkout/portal actions; cancellation is provider-driven. Property/lease records never cascade from subscription changes.",
        "Stripe event/payment IDs must prevent duplicate ledger/state side effects; webhook retries are expected.",
        "Subscription/Payment and Agency payment fields update; no Property/Deal/Lease payment rows exist.",
        "JWT access, payment-required routing and billing UI change; all agency business data remains stored even while access is blocked.",
        [
            ("Expired subscription invalidates an existing JWT", "Security", "P0", "Agency user", "Issue a valid JWT, expire/cancel the agency subscription, then call authenticated endpoint.", "AgencyJWTAuthentication rejects the previously valid token.", "No business row changes from rejected request; subscription state remains expired.", "Authenticated call returns 401 with inactive/expired message.", "User is redirected to login/payment-required and protected data clears from view.", "No destructive tenant data deletion."),
            ("Invalid Stripe signature is rejected", "Security", "P0", "External caller", "POST realistic webhook with missing/incorrect signature.", "Event is rejected before agency/subscription mutation.", "No Subscription/Payment/Agency changes.", "POST webhook returns 400 authentication/validation response.", "Billing UI unchanged.", "No email/access change."),
            ("Duplicate Stripe webhook is idempotent", "Idempotency", "P0", "Stripe webhook system", "Deliver identical signed event ID/payload twice.", "Second delivery does not duplicate payment records or incorrectly advance/reverse status.", "One logical provider event/payment effect.", "Both requests acknowledged safely; no 500.", "Billing history shows one event/payment.", "Access gate changes once."),
            ("Stripe unavailable during checkout", "Recovery", "P1", "Agency Owner", "Mock timeout/5xx/invalid response and missing configuration.", "No local paid state is granted; user receives safe retry/configuration error.", "Subscription/Agency payment state unchanged.", "Missing config 503; provider failure 502 according to view.", "Billing page shows actionable failure and retains plan selection.", "No checkout redirect or property-payment behavior."),
            ("Property transactions never invoke Stripe", "Regression", "P0", "Agency user/Public visitor", "Create property, inquiry, site visit, deal, accepted offer and lease while tracing network/provider mocks.", "No Stripe checkout/webhook/payment mutation occurs for real-estate transaction flow.", "Subscription tables unchanged.", "Only explicit SaaS billing endpoints call Stripe.", "No pay-now button appears in property/deal/lease flows.", "Cash/direct bank handling remains outside current system."),
        ],
    ),
    spec(
        "admin", "25_Platform_Admin", "Platform Administration", "ADMIN",
        "Let a super admin inspect/update agencies, suspend access, and view platform summary while keeping tenant staff out.",
        "Super Admin only",
        "/platform-admin",
        "GET/PATCH /api/operations/platform-agencies/; GET /api/operations/admin/summary/",
        "Agency, AgencyUser, Property, AuditLog, Subscription",
        "Super admin and regular owners; multiple agencies with data; paid/unpaid/suspended states.",
        "Agency status/payment/public flags and search terms; tenant IDs.",
        "Authentication/entitlement, public storefront, reports, audit logs",
        "Search agencies, inspect one, suspend it, verify internal/public access, then restore it.",
        "Only super admin can act; suspension/public/payment changes take effect across auth and public visibility without deleting tenant data.",
        "Reject regular staff, unsupported protected mutations, invalid payment/status combinations, self-created tenant links and missing agencies.",
        "ROLE_SUPER_ADMIN or Django superuser only; agency users receive 403 even if they guess routes.",
        "Platform search across name/license only for super admin.",
        "PlatformAgencyViewSet exposes no delete; agencies are updated/suspended rather than deleted.",
        "Repeated same update is safe; paid_at initializes when marking paid if absent.",
        "Agency fields and AuditLog update; child users/properties/leads remain.",
        "JWT entitlement, public site/listings, billing status and platform metrics change.",
        [
            ("Regular owner cannot access platform agencies", "Permission", "P0", "Agency Owner", "List/detail/update platform-agencies and call admin summary.", "All requests are forbidden with no data leak.", "No rows change.", "Endpoints return 403.", "Platform page/navigation unavailable; direct route shows forbidden.", "No audit entry for rejected reads/writes."),
            ("Suspending an agency blocks every surface", "End-to-End", "P0", "Super Admin", "Suspend Agency A, then test existing JWT, login, public agency/property/customer endpoints and short links.", "All tenant staff/public access gates behave consistently while data remains retained.", "Agency status changes; business rows untouched.", "Platform PATCH 200; subsequent protected/public calls rejected/404.", "Internal users are signed out/blocked; storefront unavailable.", "Scheduler should not process/send tenant public activity if eligibility excludes it."),
            ("Marking paid stamps paid_at once", "Idempotency", "P1", "Super Admin", "PATCH unpaid agency to paid twice.", "paid_at is set on first transition and not needlessly changed on identical second update.", "Agency payment_status paid; one stable paid_at; audit behavior recorded.", "Both PATCH calls return 200.", "Platform table shows paid state.", "Auth entitlement changes once."),
        ],
    ),
    spec(
        "localization", "26_Localization", "Nepal Localization and Bikram Sambat", "LOC",
        "Switch Nepali/English interfaces and BS/AD dates, format Nepali digits, NPR lakh/crore, Nepal phones/addresses, localized PDFs and message templates in Asia/Kathmandu.",
        "Agency Owner/Manager configure; all staff and public visitors consume preferences",
        "/settings localization controls; dates/numbers throughout agency UI/storefront/generated assets",
        "GET/PATCH /api/agencies/localization/ plus property/distribution/appointment APIs consuming localized fields",
        "AgencyLocalizationSettings and formatted serializer/helper output",
        "Known BS/AD reference dates including Nepali New Year; Nepal phone/address/price fixtures; English/Nepali settings.",
        "language, date_system, use_nepali_digits, timezone Asia/Kathmandu, currency style, message templates.",
        "Agency settings, all date/number UI, property distribution PDF/captions, email/messages",
        "Switch localization preferences and verify known dates, numbers, addresses, messages and generated documents across both surfaces.",
        "Known conversions round-trip; stored business values remain canonical while presentation changes; Nepal timezone is enforced.",
        "Reject non-Nepal timezone, invalid date/mode/language, malformed phone/template variables and out-of-supported BS range safely.",
        "Tenant settings apply only to that agency; public sees resolved agency preference without access to settings mutation.",
        "N/A list; verify all rendered consumers consistently read the same agency setting.",
        "Localization settings may reset/update, but business records are never deleted or rewritten solely on display switch.",
        "Conversion/format functions are pure and repeated calls stable.",
        "Only settings row changes; property/date/price raw values remain unchanged.",
        "UI labels/digits, PDFs, captions, alerts and phone links/display change—not underlying workflow state.",
        [
            ("Known Nepali New Year conversion round-trip", "Boundary", "P0", "QA tester", "Convert documented AD New Year reference to BS and back using endpoint/helper consumers.", "Result equals the known BS date and returns to the exact original AD date.", "No business rows change.", "Localization endpoint returns deterministic conversion response.", "Every date widget/document displays the same result.", "Reminder/expiry instants remain unchanged."),
            ("Non-Nepal timezone is rejected", "Validation", "P0", "Agency Owner", "PATCH timezone to UTC, Asia/Kolkata and arbitrary string.", "Serializer rejects anything outside the supported Nepal timezone rule.", "Settings timezone remains Asia/Kathmandu.", "PATCH returns 400.", "Settings shows field error and keeps prior selection.", "Appointments/reminders do not shift."),
            ("Lakh/crore and Nepali digit formatting", "Functional", "P1", "Agency/Public user", "Format boundary values around 1 lakh and 1 crore with Nepali digits on/off.", "Labels/rounding follow configured formatter; raw Decimal API values remain accurate.", "No rows change.", "Serializer/helper output is consistent where formatted fields are exposed.", "Cards, details, brochures and captions agree.", "Reports use raw numbers for calculations."),
            ("Localization switch does not mutate business data", "Regression", "P0", "Agency Owner", "Capture hashes/values of property/lead/appointment records, switch language/date/digits twice, compare.", "Only localization settings/presentation change.", "Property/Lead/Appointment rows unchanged.", "PATCH settings succeeds; GET business APIs retain canonical values.", "Presentation switches and reverses cleanly.", "No new history/automation events."),
        ],
    ),
    spec(
        "jobs", "27_Background_Jobs", "Scheduler and Background Jobs", "JOB",
        "Run time-based listing freshness, lead automation, site-visit reminders, task reminders, saved-search alerts and scheduled social publishing from the Docker scheduler loop.",
        "System scheduler; managers observe outputs",
        "No direct UI entry except automation process/dashboard and resulting notifications/statuses",
        "Management commands: process_listing_freshness; process_lead_automation; send_due_reminders; send_task_reminders; send_saved_search_alerts; publish_scheduled_posts",
        "Properties, Leads, SiteVisits, Tasks, SavedSearches, SocialPosts, Notifications and error/timestamp fields",
        "Docker scheduler running; records before/at/after each threshold; mocked email/Meta failures; two tenants.",
        "Due timestamps, last-sent markers, retryable errors, inactive agencies/users and empty queues.",
        "Database, SMTP, Meta Graph, all time-based product promises",
        "Seed due work across every command, run one scheduler cycle twice, and compare processing/output/error isolation.",
        "Each eligible item is processed once per logical window; empty queues succeed; one failure does not corrupt or duplicate unrelated work.",
        "Handle invalid/stale references, database/provider/email failure, inactive tenant, concurrent invocation and partial batches without false success.",
        "Commands run as trusted system but must retain tenant eligibility/scoping in query logic.",
        "Command counters/logs match selected/processed/skipped/failed records.",
        "Jobs do not delete source entities; they update state/markers and add notifications/events as designed.",
        "Every command must be rerun safely; concurrency locking is limited and must be stress-tested.",
        "Due record fields, errors, notifications/events and provider statuses update transactionally per item where implemented.",
        "Public visibility, reminders, assignments, alerts and social content change even without a foreground request.",
        [
            ("Empty scheduler cycle succeeds", "Background Job", "P1", "Scheduler", "Ensure no record is due and execute every management command.", "All commands exit 0 with zero-work counts and no error.", "No rows change.", "No HTTP API; command output is accurate.", "No unexpected notifications/status changes.", "No external email/Meta calls."),
            ("One malformed record does not abort unrelated batch", "Recovery", "P0", "Scheduler", "Create one invalid/stale due record among valid due records and run the command.", "Behavior is per-item safe where implemented; valid items process and failure is logged. If whole batch aborts, document as risk.", "Valid rows update once; failed row retains retryable/error state; no partial corrupt writes.", "Command exit/output identifies failure according to implementation.", "Managers can distinguish processed vs failed items.", "External calls for valid items still occur once."),
            ("Concurrent scheduler invocation", "Concurrency", "P0", "Two scheduler processes", "Start the same due command simultaneously against identical due records.", "No duplicate reminder/post/assignment should occur. Current simple scheduler/locking limitations must be documented if duplicates appear.", "Inspect unique markers/events/notifications/provider IDs.", "Both commands terminate safely; database integrity errors are handled.", "Users receive one logical side effect.", "No double remote publish/email."),
            ("Inactive agency records are skipped where eligibility requires", "Security", "P0", "Scheduler", "Make due freshness/search/social/reminder records for suspended Agency A and active Agency B.", "Public/communication jobs do not revive or expose suspended tenant; active tenant continues.", "A public state/notifications remain appropriately unchanged; B processes.", "Command logs skipped/processed counts.", "No A customer/agent receives public activity.", "No Meta/email for A."),
        ],
    ),
]


def _standard_checks(item):
    """Create implementation-specific baseline cases for every operational feature."""
    return [
        {
            "title": f"Complete the primary {item['name']} workflow",
            "type": "Functional / Positive", "priority": "P0", "severity": "Critical",
            "role": item["roles"].split(";")[0],
            "action": item["primary_action"], "expected": item["primary_result"],
            "db": item["db_impact"],
            "api": f"Use the documented routes: {item['endpoints']}. Successful create operations return 201, reads/updates/actions 200, and deletes 204 where provided; capture exact endpoint-specific response.",
            "ui": f"Start at {item['ui']}. The visible state must match the saved API response after refresh.",
            "side": item["side_effects"], "auto": "Yes",
        },
        {
            "title": f"Reject invalid or missing {item['name']} inputs",
            "type": "Negative / Validation", "priority": "P0", "severity": "High",
            "role": item["roles"].split(";")[0],
            "action": item["validation"],
            "expected": "Every invalid variant has a deterministic field/non-field error, no 500 response, and no partial business mutation.",
            "db": "No new row or partial update for the rejected request; compare affected tables before/after.",
            "api": "Expected 400 for validation/choice/relationship errors, 404 for scoped missing resources, and never an unhandled 500.",
            "ui": "Field or safe page-level error is visible; entered values remain available for correction; no false success state.",
            "side": "No notification, audit-success, email, event, background marker or external call should be produced by a rejected mutation.",
            "auto": "Yes",
        },
        {
            "title": f"Enforce {item['name']} role permissions",
            "type": "Permission / Security", "priority": "P0", "severity": "Critical",
            "role": "Exercise every listed role plus an unauthenticated caller",
            "action": item["permission_rule"],
            "expected": "Allowed roles succeed only within their documented scope; forbidden roles cannot bypass the UI by calling the API directly.",
            "db": "Forbidden requests leave all business and audit-success rows unchanged.",
            "api": "401 without required authentication; 403 for role denial; scoped 404 where queryset hiding is implemented.",
            "ui": "Controls/routes reflect permission, but server enforcement remains authoritative.",
            "side": "No data, notification or external side effect from denied requests.",
            "auto": "Yes",
        },
        {
            "title": f"Prevent cross-tenant access in {item['name']}",
            "type": "Security / IDOR", "priority": "P0", "severity": "Critical",
            "role": "Agency A authenticated user and Agency B fixture",
            "action": "Substitute Agency B object IDs, related IDs, slugs, files and filter parameters into every Agency A list/detail/create/update/delete/action request.",
            "expected": "No Agency B record, count, filename, validation detail or relationship is exposed or changed.",
            "db": "No cross-agency foreign key or row mutation; Agency B before/after snapshot is identical.",
            "api": "Scoped list is empty/excludes B; detail/action returns 404 or explicit permission denial according to current view.",
            "ui": "No hidden tenant value appears in selectors, URLs, caches or error messages.",
            "side": "No notification/email/event/audit-success in either tenant.",
            "auto": "Yes",
        },
        {
            "title": f"Verify {item['name']} list, search and filter scoping",
            "type": "API / UI", "priority": "P1", "severity": "High",
            "role": item["roles"].split(";")[0],
            "action": item["filter_rule"],
            "expected": "Result membership, count, ordering and empty state are correct for combined valid filters and invalid/missing filters fail safely.",
            "db": "Read-only; no timestamps or history should change merely by listing/filtering.",
            "api": "GET returns 200 with stable scoped pagination/shape and no private facet leakage.",
            "ui": "Filter state, URL/query, counts and cards/table rows match the response; clear/reset restores default scope.",
            "side": "No notification/external call; analytics only if explicitly implemented for that view.",
            "auto": "Yes",
        },
        {
            "title": f"Test duplicate submission and idempotency for {item['name']}",
            "type": "Idempotency / Concurrency", "priority": "P1", "severity": "High",
            "role": item["roles"].split(";")[0],
            "action": f"Double-click/submit identical requests sequentially and concurrently. Current rule: {item['duplicate_rule']}",
            "expected": "Final row/side-effect count matches current uniqueness/deduplication design; ambiguous duplicate acceptance is recorded as current behavior or defect, not silently assumed.",
            "db": "Inspect unique keys, row counts, histories, notifications and external IDs after both requests.",
            "api": "Responses are stable and never expose integrity-error 500; duplicate-specific 400/409 or safe reuse is accepted where implemented.",
            "ui": "Submitting indicator reduces accidental duplicates and refreshed state converges with server truth.",
            "side": "Emails, provider calls, alerts and audit events occur at most once per intended logical action where deduplication exists.",
            "auto": "Maybe",
        },
        {
            "title": f"Verify deletion and dependency behavior for {item['name']}",
            "type": "Database / Delete", "priority": "P1", "severity": "High",
            "role": "Agency Owner or Agency Manager",
            "action": f"Delete an unreferenced record, a referenced record, a foreign-tenant record, and an already-deleted ID. Current rule: {item['delete_rule']}",
            "expected": "Deletion/soft deletion/protection follows actual on_delete and view permission behavior with no orphan or partial cascade.",
            "db": "Inspect target, foreign keys, protected parents, cascade children, files and AuditLog before/after; rollback on failure.",
            "api": "204 for accepted delete; 403 agent denial; 404 repeated/foreign ID; dependency error must be handled rather than 500.",
            "ui": "Confirmation and dependency error are clear; lists refresh without stale rows.",
            "side": "Unrelated tenant/business records and public data remain unchanged unless directly dependent.",
            "auto": "Maybe",
        },
        {
            "title": f"Recover cleanly from API/network failure during {item['name']}",
            "type": "Error Handling / Recovery", "priority": "P1", "severity": "High",
            "role": item["roles"].split(";")[0],
            "action": "Simulate timeout, offline browser, 500 response, expired access token and failed refresh while submitting the primary workflow.",
            "expected": "UI never reports false success; safe retry does not corrupt state; expired JWT follows single refresh then login redirect on failure.",
            "db": "Accepted server transaction is either fully committed or absent; compare after retry for duplicates.",
            "api": "15-second client timeout, one refresh attempt on 401, retry with new Bearer token, then safe failure/redirect.",
            "ui": "Loading state clears, actionable error appears, form data is preserved where safe, and refreshed truth is displayed.",
            "side": "No duplicate notification/email/external action after uncertain retry unless current endpoint lacks idempotency; flag that risk.",
            "auto": "Maybe",
        },
    ]


def build_feature_cases():
    cases = []
    for item in FEATURES:
        item_cases = _standard_checks(item)
        for sp in item["specials"]:
            title, typ, priority, role, action, expected, db, api, ui, side = sp
            item_cases.append({
                "title": title, "type": typ, "priority": priority,
                "severity": "Critical" if priority == "P0" else "High",
                "role": role, "action": action, "expected": expected, "db": db,
                "api": api, "ui": ui, "side": side,
                "auto": "Yes" if typ not in {"External Integration", "Recovery"} else "Maybe",
            })
        for index, c in enumerate(item_cases, 1):
            steps = (
                f"1. Prepare: {item['prerequisites']}\n"
                f"2. Sign in/use role: {c['role']}.\n"
                f"3. Navigate to {item['ui']} and capture the initial UI/API/database state.\n"
                f"4. Execute: {c['action']}\n"
                f"5. Inspect the network request/response against: {item['endpoints']}.\n"
                f"6. Refresh/re-query the feature and related modules: {item['dependencies']}.\n"
                f"7. Verify affected models ({item['models']}) and all listed side effects."
            )
            cases.append({
                "id": f"{item['prefix']}-{index:03d}", "module": item["name"],
                "feature": item["name"], "subfeature": c["title"],
                "scenario": c["action"], "title": c["title"], "type": c["type"],
                "priority": c["priority"], "severity": c["severity"],
                "role": c["role"], "preconditions": item["prerequisites"],
                "data": item["data"], "environment": ENVIRONMENT,
                "steps": steps, "expected": c["expected"], "db": c["db"],
                "api": c["api"], "ui": c["ui"], "side": c["side"],
                "related": item["dependencies"],
                "postconditions": "Restore mutated fixture state where required; retain evidence (request/response, screenshots, SQL counts and logs).",
                "actual": "", "status": "Not Run", "bug": "",
                "automation": c["auto"],
                "notes": "Current implementation. If observed behavior differs, log a defect; do not replace actual behavior with an unimplemented recommendation.",
                "sheet": item["sheet"],
            })
    return cases


SECURITY_CASES = [
    ("SEC-001", "Authentication bypass on protected API", "No/forged JWT", "Call representative endpoints in properties, leads, operations, inbox and social modules without a valid JWT.", "All protected endpoints return 401 and no data/side effect."),
    ("SEC-002", "JWT rejected after user deactivation", "Deactivated AgencyUser", "Use a token issued before is_active becomes false.", "Authentication fails and protected data is unavailable."),
    ("SEC-003", "JWT rejected after subscription expiry", "Expired Agency", "Use a token issued while paid after entitlement expires.", "AgencyJWTAuthentication returns 401; tenant rows are retained."),
    ("SEC-004", "Cross-tenant IDOR matrix", "Agency A user", "Try Agency B IDs across every detail/action/upload/export endpoint.", "No B data or mutation; 404/403 consistently."),
    ("SEC-005", "Cross-tenant relationship injection", "Agency A manager", "Post B agent/property/lead/deal/contact/owner/account IDs into A serializers.", "Validation rejects mixed-tenant foreign keys."),
    ("SEC-006", "Agent privilege escalation by role field", "Agent", "PATCH own/other user role, agency, is_active or protected settings.", "Role/tenant/access fields remain unchanged; request forbidden/ignored safely."),
    ("SEC-007", "Owner cannot mutate payment entitlement through profile", "Agency Owner", "PATCH Agency payment_status/paid_at through /agencies/me/.", "Payment state remains server/platform controlled."),
    ("SEC-008", "Super-admin route isolation", "Agency Owner", "Call platform agencies/admin summary and attempt suspension.", "403 and no platform data/mutation."),
    ("SEC-009", "Customer token tenant isolation", "Customer A", "Use token on Agency B customer endpoints.", "Unauthorized with no B/customer data."),
    ("SEC-010", "Customer token cannot call staff APIs", "Customer", "Place customer token in Authorization/customer headers for protected staff routes.", "401; no privilege bridge."),
    ("SEC-011", "Public private-listing bypass", "Anonymous", "Guess ID/share slug/short code for draft, expired, sold, rented or unpublished property.", "All public paths exclude the record."),
    ("SEC-012", "Public private-document disclosure", "Anonymous", "Fetch verified property and inspect JSON/HTML/network/cache.", "No verification files, owner IDs, document numbers or reviewer notes."),
    ("SEC-013", "Meta webhook signature validation", "External caller", "Replay payload with invalid/missing signature.", "Rejected before persistence or provider response."),
    ("SEC-014", "Stripe webhook signature validation", "External caller", "Replay billing event with invalid/missing signature.", "Rejected before entitlement/payment mutation."),
    ("SEC-015", "Webhook replay idempotency", "External systems", "Replay identical valid Meta and Stripe events.", "One logical message/payment effect."),
    ("SEC-016", "Stored and reflected XSS inputs", "Public/staff user", "Submit script/event-handler/HTML payloads in names, notes, captions, reviews and messages.", "Payload is validated/escaped and never executes in React/Next.js/admin views."),
    ("SEC-017", "SQL/meta-character input handling", "Any caller", "Use quotes, wildcards, Unicode and SQL-like strings in search/filter/form fields.", "ORM treats values as data; no error/leak or cross-tenant result."),
    ("SEC-018", "Unsafe upload and content-type spoofing", "Agency user", "Upload executable/polyglot/oversized files as property/document images.", "Rejected or never publicly executable; production malware scanning remains not determined."),
    ("SEC-019", "Sensitive error/log leakage", "Any caller", "Trigger validation, provider, DB and 500 paths and inspect responses/logs.", "No secrets, tokens, stack traces, passwords, signatures or private file paths reach client."),
    ("SEC-020", "Password reset enumeration and token reuse", "Anonymous", "Compare known/unknown email and reuse successful token.", "Indistinguishable request response; token single use."),
    ("SEC-021", "Login/OTP/public form throttling", "Anonymous", "Burst login, resend OTP, contact/inquiry/submission calls.", "Configured throttles limit abuse without affecting tenant data."),
    ("SEC-022", "CORS/CSRF and method restrictions", "External origin", "Send disallowed methods/origins and form-encoded mutations.", "Only documented methods/auth paths succeed; no accidental state change."),
    ("SEC-023", "Local-storage token exposure review", "Browser user", "Inspect staff/customer persisted tokens, logout, XSS surface and shared-device behavior.", "Logout clears staff auth; customer long-lived token risk is documented; no token appears in URL/logs."),
    ("SEC-024", "Public facet and analytics privacy", "Anonymous", "Infer hidden stock/users via filter options, event errors, agent lists and counts.", "Only eligible public data contributes to facets/counts; inactive users hidden."),
]


E2E_CASES = [
    ("E2E-001", "Agency onboarding to first public listing", "Register owner -> satisfy SaaS entitlement -> configure Nepal settings/site -> create property/media -> verify/freshen -> publish -> public browse", "Agency and property are publicly available only after all gates; no property payment occurs."),
    ("E2E-002", "Buyer inquiry to assigned lead", "Public listing -> tracked inquiry -> lead dedup -> property interest -> rule assignment -> notification -> workspace", "One lead/interest, correct attribution/assignment/SLA and visible workspace timeline."),
    ("E2E-003", "Site visit to qualified opportunity", "Public visit request -> lead reuse -> scheduled reminder -> completion -> lead history -> deal creation", "Visit and lead state synchronize; notification/reminder and deal linkage are tenant-correct."),
    ("E2E-004", "Meta message to CRM follow-up", "Signed Facebook/Instagram webhook -> conversation -> claim -> create/link lead -> reply -> follow-up", "One inbound message, correct conversation/lead linkage and provider delivery state."),
    ("E2E-005", "Listing expiry and approved republish", "Publish -> confirm -> reminder -> expire/hide -> agent request -> manager approve -> public restore", "Public visibility is removed/restored exactly at correct stages with history."),
    ("E2E-006", "Distribution link to attributable site visit", "Generate branded asset/link -> click -> listing -> visit request -> lead/event/source metrics", "Attribution persists through link, event, lead, interest and visit."),
    ("E2E-007", "Negotiation to accepted offer", "Lead -> deal -> submitted/countered offer -> accept -> contract stage/value -> report", "Offer/deal/report values remain consistent; no Stripe property payment."),
    ("E2E-008", "Rental record workflow without payment", "Rental property -> tenant contact/owner -> lease -> documents -> tasks -> active/renewed", "Operational lease data changes only; subscription billing unaffected."),
    ("E2E-009", "Customer portal engagement", "Register/login -> save property -> saved search -> alert -> appointment -> agent notification", "Customer/tenant isolation and public eligibility hold across all steps."),
    ("E2E-010", "Meta social campaign publication", "Property distribution draft -> connect Meta -> schedule -> partial failure -> retry -> public inquiry", "One remote post per target and campaign attribution reaches the inquiry."),
    ("E2E-011", "Agency suspension across the platform", "Existing JWT/public site/scheduler due items -> super admin suspend -> repeat access/jobs -> restore", "Access/public processing stop without data loss, then recover after restore."),
    ("E2E-012", "Team invitation and agent lifecycle", "Invite -> email -> accept -> profile -> property/lead assignment -> deactivate -> public/auth restrictions", "Identity/access changes propagate while historical business references remain."),
]


EDGE_CASES = [
    ("EDGE-001", "Simultaneous JWT refresh requests", "Concurrency", "One refresh request services concurrent 401 responses."),
    ("EDGE-002", "Two managers update the same lead", "Concurrency", "Final state is understood; no corrupt history or cross-tenant change; missing optimistic locking is documented."),
    ("EDGE-003", "Two users accept/reject one offer", "Concurrency", "Offer and deal final state remain logically consistent."),
    ("EDGE-004", "Two automation workers assign one lead", "Concurrency", "One effective assignee/SLA/notification event."),
    ("EDGE-005", "Two scheduler cycles send same reminder", "Concurrency", "One logical reminder/email per window."),
    ("EDGE-006", "Two customers request one appointment slot", "Concurrency", "Capture current duplicate-booking behavior; clarification required for exclusivity expectation."),
    ("EDGE-007", "Unicode Nepali and mixed-script input", "Edge Case", "Valid Nepali text persists/searches/renders without mojibake or truncation."),
    ("EDGE-008", "Whitespace and normalized phone/email duplicates", "Boundary", "Normalization deduplicates true same lead but never merges blank/different identities."),
    ("EDGE-009", "Maximum field lengths", "Boundary", "Test max_length-1, max_length and max_length+1 for names/titles/URLs/notes where model exposes limits."),
    ("EDGE-010", "Decimal and area extremes", "Boundary", "Zero/negative/maximum precision values do not overflow, divide by zero or silently round beyond model precision."),
    ("EDGE-011", "Nepal midnight/month/year boundary", "Boundary", "Expiry, follow-up, reminder and appointment comparisons use the same timezone-aware instant."),
    ("EDGE-012", "BS supported date range boundary", "Boundary", "First/last supported conversions succeed; outside range returns validation error, not 500."),
    ("EDGE-013", "Empty tenant and zero-data dashboards", "Edge Case", "All pages/APIs show correct empty state and numeric zeros."),
    ("EDGE-014", "Large page/filter/export payload", "Performance", "Pagination/export remains bounded and tenant-correct; timeouts are handled safely."),
    ("EDGE-015", "External provider succeeds but client times out", "Recovery", "Retry does not duplicate Stripe/Meta/logical create effects where IDs/dedup exist."),
    ("EDGE-016", "Database failure mid-side-effect", "Recovery", "Transaction leaves no orphan/half-applied relationship; nontransactional external result is surfaced for reconciliation."),
]


REGRESSION_TITLES = [
    ("REG-001", "P0", "Owner login/OTP/JWT and dashboard access"),
    ("REG-002", "P0", "Expired subscription rejects existing JWT"),
    ("REG-003", "P0", "Agency A cannot access Agency B representative resources"),
    ("REG-004", "P0", "Agent cannot escalate role or manage team"),
    ("REG-005", "P0", "Property create, assign and valid publish flow"),
    ("REG-006", "P0", "Agent property draft/self-assignment enforcement"),
    ("REG-007", "P0", "Nepal area conversions and canonical filtering"),
    ("REG-008", "P0", "Verification checklist initialization and fully-verified guard"),
    ("REG-009", "P0", "Expired listing hidden and approved republish restored"),
    ("REG-010", "P0", "Public inquiry dedup and lead automation assignment"),
    ("REG-011", "P0", "Agent cannot access another agent lead workspace"),
    ("REG-012", "P0", "Meta webhook signature and idempotent ingestion"),
    ("REG-013", "P0", "Site visit completion updates lead history"),
    ("REG-014", "P0", "Accepted offer updates deal contract/value"),
    ("REG-015", "P0", "Customer token tenant isolation"),
    ("REG-016", "P0", "Public/private listing and verification-data separation"),
    ("REG-017", "P0", "Stripe only gates SaaS subscription, never property transaction"),
    ("REG-018", "P0", "Super-admin-only suspension blocks internal/public access without deletion"),
    ("REG-019", "P1", "Property media primary normalization and distribution assets"),
    ("REG-020", "P1", "Short-link attribution reaches inquiry/site visit"),
    ("REG-021", "P1", "Task/appointment assignment notifications"),
    ("REG-022", "P1", "Saved-search alert and reminder jobs are idempotent"),
    ("REG-023", "P1", "Social partial-failure retry skips successful target"),
    ("REG-024", "P1", "Reports/matching/compare remain tenant scoped"),
    ("REG-025", "P1", "Localization switch preserves canonical business data"),
    ("REG-026", "P2", "Website submission and review moderation"),
    ("REG-027", "P2", "Contact import idempotency and owner relationships"),
    ("REG-028", "P2", "Lease/document operations have no payment side effects"),
]


def make_suite_case(case_id, sheet, module, title, action, expected, typ, priority, role, related):
    return {
        "id": case_id, "module": module, "feature": module,
        "subfeature": title, "scenario": action, "title": title, "type": typ,
        "priority": priority, "severity": "Critical" if priority == "P0" else "High" if priority == "P1" else "Medium",
        "role": role, "preconditions": "Use the reusable two-tenant QA data matrix and current Docker test environment.",
        "data": "Agency A/B, representative role accounts, public/private records, valid/invalid tokens and deterministic timestamps.",
        "environment": ENVIRONMENT,
        "steps": f"1. Reset/seed deterministic fixtures.\n2. Capture initial database/API/UI state.\n3. Execute: {action}\n4. Inspect response, UI, database, logs and downstream modules.\n5. Repeat with tenant/role/failure variation where stated.\n6. Compare against the pass condition.",
        "expected": expected,
        "db": "Only rows explicitly required by the workflow may change; tenant B and unrelated records remain identical. Verify counts, foreign keys, histories, timestamps and rollback.",
        "api": "Use actual endpoints named in the relevant feature sheet/guide. Verify 2xx success and 400/401/403/404/409/429/5xx recovery behavior as applicable.",
        "ui": "UI must reflect server truth after refresh, show safe errors, and expose no unauthorized data.",
        "side": "Verify notifications, email, scheduler markers, audit/events and external provider calls separately.",
        "related": related, "postconditions": "Retain evidence and restore mutable provider/test fixtures.",
        "actual": "", "status": "Not Run", "bug": "", "automation": "Yes" if typ not in {"External Integration", "Recovery"} else "Maybe",
        "notes": "Current implementation unless explicitly labeled Requires clarification.", "sheet": sheet,
    }


def build_special_suites():
    cases = []
    for cid, title, role, action, expected in SECURITY_CASES:
        cases.append(make_suite_case(cid, "28_Security", "Security and Permissions", title, action, expected, "Security", "P0", role, "Authentication, tenant scoping, all protected/public modules"))
    for cid, title, action, expected in E2E_CASES:
        cases.append(make_suite_case(cid, "29_End_to_End", "End-to-End Journeys", title, action, expected, "End-to-End", "P0", "All roles in journey", "Cross-feature product flow"))
    for cid, title, typ, expected in EDGE_CASES:
        cases.append(make_suite_case(cid, "31_Edge_Concurrency", "Edge Cases and Concurrency", title, title, expected, typ, "P0" if typ in {"Concurrency", "Recovery"} else "P1", "Relevant concurrent/system roles", "Database transactions, scheduler, external providers"))
    feature_case_map = {c["title"]: c for c in build_feature_cases()}
    for cid, priority, title in REGRESSION_TITLES:
        source = next((c for c in build_feature_cases() if title.lower().split(" and ")[0] in c["title"].lower()), None)
        action = f"Execute the full high-value regression path: {title}."
        expected = "The previously accepted current implementation behavior passes at API, database, UI and side-effect layers with no tenant leak or new regression."
        if source:
            expected = source["expected"]
        cases.append(make_suite_case(cid, "30_Regression", "Regression Suite", title, action, expected, "Regression", priority, "Relevant role matrix", "Core authentication, CRUD, permissions, workflows and integrations"))
    return cases


ALL_CASES = build_feature_cases() + build_special_suites()


def summary():
    by_sheet = Counter(c["sheet"] for c in ALL_CASES)
    return {
        "generated": TODAY,
        "feature_areas": len(FEATURES),
        "sheets_with_cases": len(by_sheet),
        "total_cases": len(ALL_CASES),
        "p0": sum(c["priority"] == "P0" for c in ALL_CASES),
        "p1": sum(c["priority"] == "P1" for c in ALL_CASES),
        "positive": sum("Positive" in c["type"] or c["type"] == "Functional" for c in ALL_CASES),
        "negative": sum("Negative" in c["type"] or "Validation" in c["type"] for c in ALL_CASES),
        "security": sum(any(x in c["type"] for x in ("Security", "Permission", "IDOR")) for c in ALL_CASES),
        "e2e": sum(c["sheet"] == "29_End_to_End" for c in ALL_CASES),
        "by_sheet": dict(sorted(by_sheet.items())),
    }


UNKNOWNS = [
    "Exact production deployment target, object storage, backup, malware scanning and audit-retention controls are not present in the reviewed repositories.",
    "Public customer token expiry/rotation is not evident; current storefront stores a long-lived token in localStorage.",
    "Appointment double-booking prevention is not clearly enforced by a database constraint or locking workflow.",
    "Optimistic locking/version checks are not evident for concurrent lead, deal, offer or property updates.",
    "The Docker scheduler is a polling loop; durable queueing, distributed locking, retries and production monitoring are not determined.",
    "Production Meta app/webhook/OAuth configuration and real provider permissions must be validated in a controlled external test environment.",
    "Some validation expectations (for example lease date ordering and upload malware scanning) require execution/clarification if not enforced by serializers or infrastructure.",
]


PROBABLE_RISKS = [
    "Concurrent state-changing requests can use last-write-wins because entity versioning/optimistic locking is not evident.",
    "Long-lived customer portal tokens in localStorage increase shared-device and XSS blast radius.",
    "Repeated non-idempotent create/reply/publish requests after an ambiguous timeout can duplicate records or external effects unless endpoint-specific deduplication applies.",
    "File upload validation is application-level; production malware scanning/private object access is not demonstrated in this codebase.",
    "A simple scheduler loop can duplicate work if multiple scheduler instances run without command-level locking.",
]

